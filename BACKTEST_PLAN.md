# Sequoia-X V2 LSTM 项目全景与综合回测计划书

> **项目全称**: Sequoia-X V2 多模型 A 股月度选股系统
> **项目目录**: `017_workbuddy/004_sequoia-x/sequoia_x/model_selection_v2/`
> **核心目标**: 每月从 ~3000 只 A 股中选出未来 20 日超额收益最高的 10~25 只股票
> **当前阶段**: 模型验证已完成（2026-07-29），回测框架搭建中
> **撰写日期**: 2026-07-30
> **修订**: 2026-07-31 — 新增 §3.6-3.7 小三步验证+大三步计划、§4.4 工程实践铁律、§22-23 验证链+优化方向

---

# 第一部分：项目背景

---

## 〇、项目起源与演进

### 0.1 什么是 Sequoia-X

Sequoia-X 是一套 A 股量化交易研究系统，由多个独立项目组成。本项目（004_sequoia-x）专注于**多模型股票排序**——不是预测股价涨跌的绝对数值，而是对全市场股票进行「谁更可能跑赢大盘」的排序。

这类似于考试排名而非预测分数：我们不需要精确知道每只股票下月涨多少，只需要知道**哪几只股票最可能涨得比别的股票多**。

**V1（model_selection）**: 使用单个 LSTM-Transformer 深度模型，直接预测股票的未来收益率。
**V2（model_selection_v2）**: 引入四模型体系，树模型 + 深度模型互补，增加了 Walk-Forward 验证框架。

### 0.2 什么是 Walk-Forward 验证

Walk-Forward（滚动前向验证）是量化策略最严格的检验方式。它的核心思想是**模拟真实交易场景**——用过去的数据训练，在未来的数据上测试，然后不断向前推进:

```
Fold 3: 用 2020-2024 数据训练 → 在 2025 年测试（模拟站在 2025 年初的你）
Fold 4: 用 2020-2025Q1 训练 → 在 2025 Q2-Q4 测试
Fold 5: 用 2020-2025 训练 → 在 2026 H1 测试
Fold 6: 用 2020-2026Q1 训练 → 在 2026 Q2 测试
```

这样设计是为了彻底杜绝**前视偏差（look-ahead bias）**——模型在训练时"看到"了未来的数据。Walk-Forward 中的 `purge_gap = 22 个交易日` 是额外的安全保障——训练集的最后一天和测试集的第一天之间至少间隔 22 天，防止训练集的标签信息「泄露」到测试集。

### 0.3 四模型体系的设计理念

为什么用四个模型？类比医生诊断:

| 模型 | 类型 | 角色 | 类比 |
|------|------|------|------|
| T1 (XGBoost 分类器) | 树模型 | 方向判断——会涨还是跌？ | 初步筛查 |
| T2 (LightGBM 回归器) | 树模型 | 超额收益预测——跑赢大盘多少？ | 主力医生 |
| T3 (CatBoost 回归器) | 树模型 | 风险预测——波动有多大？ | 风险评估 |
| T4 (LSTM 深度学习) | 神经网络 | 超额收益预测——从历史走势中发现隐藏规律 | 专家会诊 |

**树模型（T1/T2/T3）** 擅长发现「如果 PE 低于 15 且成交量放大 50%，大概率跑赢大盘」这样的**条件规则**。
**LSTM（T4）** 擅长发现「PE 在过去 20 天加速下降 + 量能萎缩后突然爆发」这样的**时序模式**。

两者互补——树模型看「截面」（同一时间点不同股票的比较），LSTM 看「时序」（同一股票的历史演变）。

### 0.4 预测目标详解

**y1 — 5 日方向（二分类）**: 如果 5 个交易日后股价 > 今天股价，y1 = 1（涨），否则 y1 = 0（跌）。T1 预测此目标。

**y2 — 20 日超额收益（回归，连续值）**:
```
stock_ret = (20天后股价 / 今天股价) - 1
idx_ret   = (20天后沪深300 / 今天沪深300) - 1
y2 = clip(stock_ret - idx_ret, -0.5, +0.5)
```
T2 和 T4 预测此目标。裁剪到 [-0.5, +0.5] 防止极端值干扰。

**y3 — 20 日年化波动率（回归）**: 未来 20 个交易日每日收益率的标准差 × √252。T3 预测此目标。

### 0.5 特征体系（80 维 → 88 维）

特征分组（80 维基础版 + 8 维市场状态 = 88 维）:

| 组号 | 类别 | 维度 | 包含指标 |
|------|------|:----:|---------|
| 1 | 价格收益 | 8 | ret_1d/5d/10d/20d, 跳空缺口, 振幅, 涨跌幅, 5日振幅 |
| 2 | 均线偏离 | 6 | MA5/10/20/60/120 偏离度 + MA5/MA20 比值 |
| 3 | 量能 | 8 | 量比, 5日量变化, 换手率, 成交额比, 量价相关, 量能趋势, 放量标记 |
| 4 | 技术指标 | 11 | RSI, MACD(DIF/DEA/柱), 布林带, ATR, KDJ, OBV变化, ADX |
| 5 | 波动率 | 4 | 5/10/20 日年化波动率 + 短/长波动率比值 |
| 6 | 大盘关联 | 8 | Beta, 20日超额收益, 指数偏离等 |
| **6b** | **市场状态(新增)** | **8** | 大盘20/60日涨跌幅, 波动率, 波动加速度, 回撤, 均线, 上涨占比 |
| 7 | 价格形态 | 7 | 涨跌停, 连涨/连跌, 新高/新低, K线实体比 |
| 7b | 最大回撤 | 3 | 20/60日回撤, 恢复率 |
| 7c | 收益分布 | 4 | 偏度, 峰度, 上涨天数比, 非对称波动 |
| 7d | 时间日历 | 4 | 周几(sin/cos编码), 月几日, 季末标记 |
| 7e | 价格位置 | 3 | 收盘位置, 5日平均跳空, 短/长振幅比 |
| 8 | 估值 | 4 | peTTM, pbMRQ + 各自60日分位 |

**特征工程的严格规则——杜绝前视偏差**: 第 T 日的特征**只能使用第 T 日及之前已知的数据**。归一化方式是**每只股票独立、沿时间轴做 Z-score**。

---

## 一、T4 LSTM 的艰难调试——项目最深刻的一课

### 1.1 问题表象

T4（LSTM-Transformer）在 Walk-Forward 验证中，**预测值恒为常数**——对所有股票给出完全相同的预测。val_loss = 0.0045 看起来正常但预测无效——这是深度学习最危险的情况。

### 1.2 四轮误诊

**第一轮（07-22）——怀疑 Keras 序列化**: 以为 `load_model()` 破坏自定义层。改为内存模型，仍退化。❌

**第二轮（07-23）——怀疑 Dropout 推理退化**: 降 dropout→0.1→0，去 attention dropout。随机数据 OK，真实数据仍退化。❌

**第三轮（07-24）——发现单核运行**: `.bashrc` 的 `KMP_AFFINITY` 锁定单核 + TF 线程变量在导入后设置无效。修复线程后 epoch 从 29min→64s，但预测仍退化。❌

**第四轮（07-28）——找到真凶**: 

| 实验 | 配置 | pred_std | Rank IC |
|------|------|---------|---------|
| A | 纯LSTM + L2=0 | 0.045 | +0.055 ✅ |
| B | 纯LSTM + L2=1e-4 | 0.000 | 0 ❌ |
| C | LSTM+3T + L2=0 | 0.000 | 0 ❌ |
| D | 仅去Transformer | 0.037 | +0.089 ✅ |

**根因一: L2 正则化(1e-4)杀死 LSTM input kernel**——权重范数从 15.78 衰减到 0.00。

**根因二: Transformer 层稀释信号**——3 层 Self-Attention + LayerNorm 串联将 LSTM 提取的微弱时序模式逐步抹平。

**最终修复**: `lstm_l2_reg=0.0`, `num_transformers=0`, KMP_AFFINITY 清除, TF 线程显式设置。

### 1.3 为什么反复误诊——元教训

1. 没有先验证权重本身就直接修改代码
2. 单次改了多个变量，无法确定因果关系
3. 为了迎合期望选择性解释数据

后续增加三条铁律: 详尽日志+运行时自检、断点续跑、修复后必须验证。

---

## 二、2026 年市场风格切换

### 2.1 Walk-Forward 完整结果

| Fold | 测试期 | T2 Rank IC | T4 Rank IC | 方向胜率 |
|------|--------|-----------|-----------|---------|
| 3 | 2025 全年 | +0.117 | +0.071 | 55.1% |
| 4 | 2025 Q2-Q4 | +0.141 | +0.101 | 53.1% |
| 5 | 2026 H1 | **-0.258** | **-0.258** | 0.0% |
| 6 | 2026 Q2 | +0.156 | -0.091 | 0.0% |

### 2.2 y2 分布变化

| 时段 | y2 均值 | y2 标准差 | y2>0 比例 |
|------|---------|----------|----------|
| 2025 全年 | +1.2% | 0.106 | 48% |
| 2026 H1 | **-10.8%** | **0.224** | 32% |
| 2026 Q2 | **-21.7%** | **0.252** | **仅 20%** |

---

## 三、三大验证实验

### 3.1 月度 Walk-Forward（80 维 T4）

| 月份 | y2 | T4 IC | 关键 |
|------|-----|-------|------|
| 2025-08~12 | — | 均值 +0.038 | 正常 |
| 2026-01 | +2.1% | +0.066 | 风格突变守正 |
| 2026-03 | -1.1% | **+0.210** | 爆发 |
| 2026-05 | **-42.0%** | **+0.052** | 崩盘月守正 |
| 2026-06 | -21.5% | -0.153 | 二次探底 |

2026 月均 +0.037，vs 半年度 Fold 5 的 -0.258。月度重训练有效。

### 3.2 88 维 vs 80 维 T4

80 维在 4/6 月领先，均值 +0.081 vs +0.067。LSTM 不需要显式市场特征。

### 3.3 T2 月度（88 维）

| 月 | T2 IC | T4 80维 IC | 胜者 |
|----|-------|-----------|:--:|
| 1月 | -0.135 | +0.066 | T4 |
| 3月 | **+0.272** | +0.210 | T2 |
| 5月 | -0.051 | +0.052 | T4 |
| 6月 | **+0.274** | -0.153 | T2 |
| **均值** | **+0.094** | +0.037 | T2 |

T2 爆发强，T4 防守稳。T2+T4 Rank 融合 = 均值 +0.065，最差月仅 -0.035。

### 3.4 扩窗口 vs 滚动窗口

扩窗口在 3 月崩盘（-0.072 vs 滚动的 +0.223），被否决。

### 3.5 6 月 vs 12 月训练窗口

6 月窗口 2026 均值 -0.026，12 月窗口 +0.094。12 月为最优。

### 3.6 小三步完整验证报告（2026-07-29 执行）

**小三步目标**：验证三个独立改进在 IC 层面的效果——不涉及回测收益，只看选股排序质量。

#### 第一步：月度 Walk-Forward（12 月滚动 → 1 月测试）

将原 Walk-Forward 的固定 Fold 划分（Fold3=2025全年, Fold5=2026H1）改为逐月滚动窗口，每月用前 12 个月数据重新训练。

| 月份 | y2 均值 | 80维 T4 Rank IC | 88维 T4 Rank IC | 方向 |
|------|---------|:--------:|:--------:|:--:|
| 2025-08 | -3.8% | +0.005 | — | 中性 |
| 2025-09 | -1.7% | **+0.090** | — | ✅ |
| 2025-10 | +3.4% | -0.002 | — | 中性 |
| 2025-11 | -0.1% | +0.045 | — | ✅ |
| 2025-12 | +2.4% | +0.053 | — | ✅ |
| 2026-01 | +2.1% | +0.066 | +0.049 | ✅ |
| 2026-02 | -0.5% | **+0.104** | +0.078 | ✅ |
| 2026-03 | -1.1% | **+0.210** | **+0.223** | ✅🔥 |
| 2026-04 | -1.8% | **-0.056** | -0.084 | ❌ |
| 2026-05 | **-42.0%** | +0.052 | +0.030 | ✅（崩盘月守正） |
| 2026-06 | -21.5% | **-0.153** | **-0.024** | ❌→⚠️（88维改善） |

> **80 维 2026 月均 IC = +0.037，9/11 月 > 0（82%）**
> **88 维 2026 月均 IC = +0.045，4/6 月 > 0（67%）**
>
> 对比改进前 Fold5（2026H1）IC = **-0.258**，月度 WF 将其从不可用提升到可用。

#### 第二步：88 维市场状态特征（80 → 88 维 T4）

新增 8 维大盘环境特征（20/60 日涨跌幅、波动率、回撤、均线关系、上涨占比），测试 LSTM 是否需要显式市场特征。

| 月份 | 88 维 IC | 80 维 IC | 差值 | 结论 |
|------|:-----:|:-----:|:-----:|:----:|
| 2026-01 | +0.049 | +0.066 | -0.017 | 80 维优 |
| 2026-02 | +0.078 | +0.104 | -0.026 | 80 维优 |
| 2026-03 | **+0.223** | +0.210 | +0.013 | 持平 |
| 2026-04 | -0.084 | -0.056 | -0.028 | 80 维优 |
| 2026-05 | +0.030 | +0.052 | -0.022 | 80 维优 |
| 2026-06 | **-0.024** | -0.153 | **+0.129** 🔥 | 88 维优 |
| **均值** | **+0.045** | **+0.037** | — | — |

> **80 维在 4/6 月领先**。88 维仅在 2026-06 极端月份有显著改善（IC 从 -0.153 提升到 -0.024）。
>
> **结论：LSTM 能从 120 步时序中隐式推断市场状态，显式市场特征反而引入噪声。** 这与树模型相反——T2(88维) 月均 IC=+0.094，远超 T4(80维) 的 +0.037，因为树模型只能看截面数据，必须将市场状态编码为特征。
>
> **设计决策：T4 保持 80 维，T2 使用 88 维。非对称设计是正确的。**

#### 第三步：T2+T4 Rank 融合回测（7/29 晚间）

使用 T2(88维)+T4(80维) Rank 融合策略，对 2025-08 至 2026-06 的 11 个月进行完整月度回测。**这是小三步中唯一涉及收益层面的验证。**

| 周期 | TOP_N | 总收益 | 年化 | 夏普 | 最大回撤 | 月胜率 |
|------|:-----:|--------|------|------|----------|:----:|
| **2025年(5月)** | 10 | **+10.4%** | +26.8% | 1.93 | -0.96% | 60% |
| **2025年(5月)** | 15 | **+16.5%** | +44.4% | 3.32 | 0.00% | 80% |
| **2025年(5月)** | 20 | **+25.8%** | **+73.5%** | **4.14** | 0.00% | 80% |
| 2025年(5月) | 25 | +4.5% | +11.2% | 0.73 | 0.00% | 80% |
| | | | | | | |
| 2026年(6月) | 10 | -40.3% | -64.4% | -0.41 | -65.2% | 67% |
| 2026年(6月) | 15 | -51.3% | -76.3% | -0.85 | -68.3% | 50% |
| 2026年(6月) | 20 | -50.9% | -75.9% | -0.69 | -70.6% | 50% |
| 2026年(6月) | 25 | -54.4% | -79.2% | -0.91 | -69.8% | 50% |
| | | | | | | |
| **全周期(11月)** | 10 | -42.5% | -45.3% | -0.40 | -65.4% | 55% |
| **全周期(11月)** | 15 | -46.7% | -49.7% | -0.42 | -69.9% | 64% |
| **全周期(11月)** | 20 | -50.7% | -53.8% | -0.52 | -71.1% | 55% |
| **全周期(11月)** | 25 | -49.5% | -52.6% | -0.53 | -70.4% | 64% |

**月度收益走势（top_n=15，最能反映模型真实表现）**：

```
2025-08:  -5.6%  ████
2025-09:  +4.3%  ████████████
2025-10:  +3.7%  ███████████
2025-11:  +0.3%  ██
2025-12: +11.6%  ██████████████████████████  ← 峰值
2026-01: +13.3%  ████████████████████████████
2026-02:  +5.3%  █████████████
2026-03: +29.9%  ███████████████████████████████████████████████  ← 最高
2026-04:  -4.4%  ██████████
2026-05: -37.7%  ██████████████████████████████████████████████████████████████████████  ← 雪崩
2026-06: -49.4%  ██████████████████████████████████████████████████████████████████████████████████████  ← 崩溃
```

**小三步核心发现**：

1. ✅ 月度 WF + T2+T4 融合在 2025 年表现出色（年化 +73.5%，夏普 4.14），证明了模型在正常市场下的有效性
2. ❌ 2026 年 5-6 月两个月合计亏损 -78~-89%，吃掉全部利润并巨亏
3. ❌ 风控完全失效——5 种风控模式（裸融合/+T1/+T3/+市场状态/全风控）收益完全相同，风控链路断裂
4. ⚠️ 市场状态检测是滞后指标——月末反弹使其显示"正常"，错过了月初暴跌

### 3.7 大三步：T4 LSTM 追加到缓存 + T2+T4 完整回测（✅ 2026-07-31 完成）

**大三步目标**：小三步的 T4 仅覆盖了部分预测（快速模拟），大三步要对 11 个月分别进行 **T4 LSTM 真实训练+预测**，然后与 T2 做 Rank 融合，跑完整的 72 组回测。回答 BACKTEST_PLAN 的核心问题——**"T2+T4 双模型集成 + 月度重训练，在真实交易数据上到底能赚多少、亏多少？"**

#### 三个步骤

| 步骤 | 内容 | 耗时 | 状态 |
|------|------|------|:----:|
| 大1 | T4 LSTM 11 个月 80 维真实训练 + 预测追加到缓存 | ~2.3h（并行） | ✅ |
| 大2 | 72 组 T2(88维)+T4(80维) Rank 融合完整回测 | ~8min | ✅ |
| 大3 | 最终分析报告 | — | ✅ |

#### 大1 技术要点

- **11 进程并行**：每月份一个独立 Worker，`TF_INTRA=2 TF_INTER=1 OMP=2`，总 CPU ~15 核
- **80 维 LSTM**：使用 `9cdff14e8a1a` 缓存 + `include_market_state=False`
- **使用优化参数**：`num_transformers=0, dropout=0.285, lr=0.0096, batch_size=32`
- **nohup 解绑**：通过 `scripts/launch_t4_parallel.sh` 启动，PPID=1
- **断点续跑**：已完成月份自动跳过（`.t4_tmp/t4_{month}.json`）
- **特征提取瓶颈**：`_extract_per_day_features` 对每只股票的全历史计算特征，每只 2-3s，2978 只耗时 ~100min/月。但并行后墙钟时间仅 ~140min

#### 大2 核心结果：T2+T4 vs 纯 T2 对比

| 指标 | 小三步 纯T2 | 大三步 T2+T4 | 变化 |
|------|:-----:|:-----:|:--:|
| **全周期总收益** | **-42.5%** | **+55.9%** | **+98.4%** 🔥 |
| 全周期年化 | -45.3% | +66.7% | +112% |
| **全周期夏普** | **-0.40** | **+2.30** | **+2.70** |
| 全周期最大回撤 | -65.4% | **-14.9%** | -50.5% |
| 2025年总收益(TOP10) | +10.4% | +2.1% | -8.3% |
| 2026年总收益(TOP10) | -40.3% | **+53.0%** | **+93.3%** |

#### 72 组全周期 TOP 配置

| 风控 | TOP_N | 总收益 | 年化 | 夏普 | 回撤 | 月胜率 |
|------|:---:|--------|------|------|------|:---:|
| M4 +IC加权 | 10 | **+61.3%** | **+73.3%** | 2.03 | -20.6% | 73% |
| M5 全风控 | 10 | +61.3% | +73.3% | 2.03 | -20.6% | 73% |
| M2 +T3仓位 | 10 | +56.8% | +67.8% | **2.33** | -15.0% | 64% |
| M0 裸融合 | 10 | +55.9% | +66.7% | 2.30 | **-14.9%** | 64% |
| M0 裸融合 | 15 | +45.2% | +53.6% | 1.84 | -16.9% | 64% |

#### 最优配置（综合评分）

| 字段 | 值 |
|------|-----|
| 风控模式 | **M5（全风控）** |
| TOP_N | **15** |
| 时段 | 2026年（6个月） |
| 总收益 | **+46.1%** |
| 年化收益 | **+127.7%** |
| 夏普比率 | **+2.61** |
| 最大回撤 | **-16.89%** |
| 月胜率 | **83%** |
| 综合评分 | 0.858 |

#### 关键发现

1. **T4 是绝对主力**：加入 T4(80维) 后，全周期从 -42.5% 翻到 +55.9%，回撤从 -65% 降至 -15%。LSTM 捕捉到了树模型看不到的时序模式
2. **风控链路已分化**：M0≠M2≠M5，五种模式不再完全相同，风控部分生效
3. **TOP_N=10 最优**：集中持仓在回测中优于分散（vs TOP_N=20/25）
4. **2025 年收益下降**：T2+T4 在 2025 年（+2.1%）不如纯 T2（+10.4%），但换来了 2026 年的巨大改善——这是"防守换进攻"的典型取舍
5. **2026-06 仍是最差月**：T4 pred_mean=-0.483，但 Rank 融合后的实际亏损仅 -14%，远优于纯 T2 的 -49%

---

## 四、重大失误记录

### 4.1 盘中 force 同步污染数据（07-29 13:18）

13:18 执行 `sync_daily(force=True)`，将 5202 只股票的盘中数据写入 `stock_daily`。晚间管线增量同步跳过这些股票（last_date 已为当日）。约 2737 只股票的 7/29 收盘价实为盘中价（偏差最大 2.38%）。LLM 选股被迫重跑 3 次。

### 4.2 退市归档误删正常股票（07-29 19:39）

管线 v2.6 的 `_archive_delisted_stocks()` 将 `stock_list` 中 2610 只被错误标记为退市的正常股票数据清空。stock_daily 从 5213 只骤降到 2603 只。手动恢复数据 2 次。

### 4.3 缓存反复失效

缓存 key 中 `n_stocks` 字段因数据恢复操作频繁变动，触发 4 次全量重建（每次 30-76 分钟）。

### 4.4 工程实践铁律

以下规范来自本项目 V2 实战教训，适用于所有代码开发和任务执行。

#### 铁律一：长时间任务必须多进程并行

任何预计运行 >1h 的任务，如果可分解为独立子任务，**必须并行执行**。

**并行原则**：

| 场景 | 串行耗时 | 并行方案 | 并行后耗时 |
|------|---------|---------|----------|
| T4 × 11 月训练 | ~11h（1h/月） | 11 进程各训 1 月 | ~1h |
| 72 组回测 | ~360min | 8 进程 | ~45min |
| 4 模型 × 11 月训练 | ~9h | 8 进程 | ~1.5h |

**实现模式**：

```bash
# 模式一：bash 循环 + nohup（适合完全独立的子任务）
for m in 2025-08 2025-09 ... 2026-06; do
    nohup python -u worker.py --month "$m" > "logs/$m.log" 2>&1 &
done

# 模式二：Python multiprocessing（适合需要汇总结果的任务）
from multiprocessing import Pool
with Pool(processes=8) as pool:
    results = pool.map(train_one_month, months)
```

**线程配置**：并行场景下，每个子进程的线程数必须调低，避免总线程数超过 CPU 核数。策略：

```
总线程 ≈ 子进程数 × (TF_INTRA + TF_INTER + OMP) ≤ CPU 核数 × 1.5

示例：11 进程 × (2+1+2) = 55 线程，36 核 × 1.5 = 54 → 刚好
反例：11 进程 × (16+8+10) = 374 线程 → 36 核 → 灾难
```

**方法**：Worker 脚本在 `import tensorflow` 之前设置 `os.environ`，模块级代码使用 `setdefault`（而非 `=` 赋值）避免覆盖：

```python
# ✅ 正确（deep_lstm.py 模块级）
os.environ.setdefault("TF_NUM_INTRAOP_THREADS", str(cfg.lstm_tf_intraop_threads))

# ❌ 错误
os.environ["TF_NUM_INTRAOP_THREADS"] = str(cfg.lstm_tf_intraop_threads)
```

**⚠️ KMP_AFFINITY 必须清除（2026-08-01 重大教训，详见 §26）**：`.bashrc` 的 `KMP_AFFINITY=granularity=fine,verbose,compact,1,0` 会把**所有进程的主线程绑定到同一核心集**。8 进程时线程恰好错开绑定（正常），16/24 进程时全部抢同一核心 → **CPU 1 核假象、训练全卡**。任何多进程并行程序（T4 / build / 回测）启动时必须：

```python
# Worker 脚本模块级（import 任何数值库之前）
os.environ.pop("KMP_AFFINITY", None)          # 清除 .bashrc 绑核（必须 pop，setdefault 无效）
os.environ["OMP_NUM_THREADS"] = "1"           # 硬赋值覆盖 .bashrc 的 36（setdefault 无效）
os.environ["OPENBLAS_NUM_THREADS"] = "1"
os.environ["MKL_NUM_THREADS"] = "1"
```

```bash
# 启动命令双保险
env -u KMP_AFFINITY -u OMP_NUM_THREADS nohup python -u worker.py ... > log 2>&1 &
```

#### 铁律二：长时间任务必须 nohup 解绑

任何预计运行 >1h 的任务，启动后**必须用 nohup 与 Claude Code 会话解绑**（PPID=1），防止 SSH 断开/会话退出中断任务。

**nohup 解绑规范**：

- ✅ 每个子进程用独立日志文件（`> log_${id}.log 2>&1`）
- ✅ 提供统一的启动/状态/终止/合并脚本（参考 `scripts/launch_t4_parallel.sh`）
- ✅ 用 PID 文件追踪进程存活状态
- ✅ 子任务完成后写入标记文件（断点续跑检测用）
- ✅ 启动命令加 `env -u KMP_AFFINITY -u OMP_NUM_THREADS`（防锁核，见铁律一/§26）

#### 铁律三：修复代码后必须验证生效

修改代码后（尤其是 Bug 修复），必须做**至少一个运行时检查**确认修复生效：

| 修复类型 | 验证方法 |
|---------|---------|
| 配置类（线程数、参数值） | `grep` 实际运行日志，确认值已变化 |
| 计算类（模型输出、IC） | 用小批量数据跑一次，验证结果在合理范围 |
| 管线类（断点续跑） | 模拟一次中断恢复 |
| 逻辑类（参数加载） | 检查日志中是否打印了修复后的消息（如"使用已保存最佳参数"） |

**禁止行为**：改完代码就声称"修好了"，不验证、不等结果。反例：T4 `search_optuna=False` 参数加载修复——改了 `deep_lstm.py` 后立即通过 `grep` 日志确认 11 个 Worker 都打印了"使用已保存最佳参数"。

#### 铁律四：断点续跑，同命令恢复

预计运行 >1h 的任务，必须实现：

- **增量保存**：每完成一个原子单元（月份/Fold/股票）→ 立即写入中间结果文件
- **启动检测**：启动时加载已有结果 → 自动跳过已完成单元
- **同命令续跑**：崩溃后重跑同一命令即可，无需额外参数

```python
# 典型模式
out_file = TMP_DIR / f"done_{unit_id}.json"
if out_file.exists():
    logger.info(f"跳过：已完成 ({out_file})")
    continue

result = do_work(unit_id)
with open(out_file, 'w') as f:
    json.dump(result, f)
```

#### 铁律五：详尽日志 + 运行时自检

日志必须能回答三个问题：

1. **跑到哪了？** — 阶段名 + 进度百分比 + 当前数据规模
2. **还要多久？** — ETA 预估，每完成一个原子单元就更新
3. **中间结果怎么样？** — 每个单元结束时打印关键指标（loss、IC、pred_std）

**启动诊断**（强制）：打印 CPU 核数、TF 线程配置、关键参数值、数据维度。

**运行时自检**（强制）：训练完成后验证：
- 模型预测值标准差 > 1e-7（防止常数预测 Bug）
- IC 值在合理范围（-1 到 1）
- 输出文件非空

**反例**：2026-07-24，TF 用 1 核跑了整晚（29min/epoch），日志没有 CPU 利用率信息，直到第二天早上才发现。

#### 铁律六：统一生产环境 Python 版本（2026-08-02 新增）

**所有运行（验证、训练、回测、模拟盘、分析、绘图）必须使用生产环境 Python**：

```bash
# ✅ 生产环境（唯一允许）
/home/zhulei/anaconda3/envs/zhulei_py312/bin/python

# ❌ 禁止（base 环境，numpy/scipy 版本不同）
python3
```

**原因（2026-08-02 实测）**：base（numpy 1.26.4/scipy 1.13.1）与 py312（numpy 2.5.1/scipy 1.18.0）
跑同一回测结果**漂移 25%**（M4+10 全周期 +757% vs +945%）——numpy/scipy 版本差异 →
rankdata ties 处理/浮点顺序 → 排名细微差异 → 选股不同 → 收益累积放大。
也导致"图与数据不一致"（plot 误用 base 跑出 +1003.76% vs 真实 +1017.39%）。

**执行要求**：
- 启动命令显式用 py312 完整路径（bash 脚本、cron、nohup 一律如此）
- 脚本启动诊断打印 `sys.executable` + numpy/scipy 版本（铁律五自检）
- 新脚本一律以 py312 为唯一运行环境；排查"结果异常/图与数据不一致"先检查 Python 环境
- 涉及：`launch_backtest_parallel.sh`（已改）、`plot_*.py`、`analyze_*.py`、`v2_*.py`、`pipeline/pipeline.py`

---

# 第二部分：综合回测计划

---

## 五、回测目标

对最终选定的 **T2(88维)+T4(80维) Rank 融合 + T1/T3/市场状态风控 + 月度重训练 + 月度调仓** 策略进行 60 组全维度回测，确定最优参数组合。

**核心原则**: 回测**必须使用当时真实的股票数据**，不能使用任何未来数据，不能用快速模拟替代真实训练。回测结果直接反映该策略在真实交易中能取得的收益。

---

## 六、数据基础与缓存

### 6.1 已有缓存（全部就绪，无需重建）

| 缓存 Hash | 维度 | 样本数 | 创建日期 | 用途 |
|-----------|:----:|--------|---------|------|
| `9cdff14e8a1a` | **80 维** | 135,795 | 07-27 | T4 训练（80维） |
| `4eca33f0b89e` | **80 维** | 135,795 | 07-27 | T4 训练（备用） |
| `d3afaeb9b691` | **88 维** | 135,846 | 07-29 | T2/T1/T3 训练 |
| `1e2404aff8dc` | **88 维** | 135,945 | 07-30 | T2/T1/T3 训练（最新） |

### 6.2 市场状态数据缓存

沪深 300 指数数据（`sh.000300`）在 `index_daily` 表中已有 **622 条**日线记录，覆盖全部回测时段。市场状态检测**不需要额外的数据下载**——直接从数据库读取即可。为进一步加速，可将 8 维市场状态特征**预计算并缓存**为一个独立的 numpy 文件（`data/cache/v2_dataset/market_state_88.npy`），避免每次回测时重复计算。

### 6.3 股票池

`engine.get_base_stock_pool()` 返回约 **2977 只**，已自动过滤板块限定 + ST + 次新 + 股价 <2 元。

### 6.4 前视偏差防控——回测数据隔离铁律

回测中**严格禁止**以下操作:

| 禁止操作 | 原因 | 正确做法 |
|---------|------|---------|
| 用全时段数据做 Z-score 归一化 | T 日的特征会隐式包含 T+1 日的信息 | 每只股票独立、沿时间轴滚动归一化（已由 features.py 实现） |
| 用 T+1 日及之后的任何数据 | 这是前视偏差 | 模型训练/预测只用 ≤T 日数据 |
| 先看到 y2 再生成预测 | 预测必须基于模型，不能偷看答案 | T4 快速模拟模式**改为真实训练模式** |
| 标注时引用未来价格 | y2 标签虽然定义为「未来 20 日收益」，但在 T 日采样时该标签已经确定 | okay——这是标签，不是特征 |

**关键变更**: 原计划的「T4 快速模拟模式」被取消。回测中 T4 必须实际训练+预测，与模拟盘运行方式完全一致。这是用户第 6 条意见的明确要求。

---

## 七、多进程并行计算架构

### 7.1 并行策略

回测涉及大量可独立并行的计算任务。采用 Python `multiprocessing` 实现多进程加速:

| 阶段 | 任务 | 可并行粒度 | 推荐进程数 | 加速比 |
|------|------|----------|:---------:|:-----:|
| 数据加载 | 加载缓存 X/y/dates | 不可并行（I/O 瓶颈） | 1 | — |
| **月度训练** | 11 个月的 T2/T4/T1/T3 训练 | **每月独立** | **8** | ~6x |
| **月度预测** | 11 个月 × 4 模型预测 | **每月×每模型独立** | **8** | ~5x |
| 回测循环 | 60 组回测 | **每组独立** | **8** | ~6x |
| 图表生成 | 60 张图 | **每张图独立** | **8** | ~6x |

### 7.2 实现方式

```python
from multiprocessing import Pool

def train_one_month(month, model_type):
    """训练单月单模型（独立进程）"""
    X_train, y_train = load_training_data(month, model_type)
    if model_type == "T2":
        return train_reg(X_train, y_train, cfg, search_optuna=False)
    elif model_type == "T4":
        return train_lstm(X_train, y_train, cfg, search_optuna=False)
    # ...

# 并行训练所有月份的所有模型
with Pool(processes=N_WORKERS) as pool:
    tasks = [(m, mt) for m in months for mt in ["T2", "T4", "T1", "T3"]]
    results = pool.starmap(train_one_month, tasks)
```

### 7.3 资源分配

| 资源 | 总量 | T4 训练占用 | 其他任务占用 | 余量 |
|------|:----:|:----------:|:----------:|:----:|
| CPU 核心 | 36 | 16 线程/Fold | 8 进程 | 12 |
| 内存 | 187 GB | ~16 GB/Fold | ~4 GB/进程 | 充裕 |
| 磁盘 | — | cache (5.7+5.2 GB) | — | 充裕 |

---

## 八、回测时间估算

### 8.1 各阶段耗时

| 阶段 | 内容 | 串行耗时 | 并行后耗时 | 备注 |
|------|------|---------|----------|------|
| 1 | 数据加载 | 30s | 30s | mmap 秒级加载 |
| 2 | T2 训练 × 11 月 | ~15min | **~3min** | 8 进程并行 |
| 3 | T4 训练 × 11 月 | ~8h | **~1.5h** | 8 进程并行，每 Fold ~40min |
| 4 | T1 训练 × 11 月 | ~11min | **~2min** | 8 进程并行 |
| 5 | T3 训练 × 11 月 | ~11min | **~2min** | 8 进程并行 |
| 6 | 全股票池预测 × 11 月 | ~30min | **~5min** | 8 进程并行 |
| 7 | 60 组回测计算 | ~10min | **~2min** | 8 进程并行 |
| 8 | 60 张图表生成 | ~3min | **~30s** | 8 进程并行 |
| 9 | CSV/JSON 输出 | 5s | 5s | 单进程 |
| **总计** | | **~10h** | **~2h** | **并行后** |

### 8.2 并行加速的关键

T4 训练是绝对瓶颈（占串行时间的 80%）。8 进程并行意味着同时训练 8 个月份的 T4 模型（每个用 16 TF 线程），将 8 小时压缩到 1.5 小时。内存占用约 8 × 16 GB = 128 GB，在 187 GB 总量内安全。

---

## 九、选股逻辑（每月末执行一次）

### 9.1 模型训练

每个月末最后一个交易日，用前 12 个月的滚动窗口训练:

| 模型 | 目标 | 代码 | 训练数据 |
|------|------|------|---------|
| T2 (LightGBM) | y2 | `tree_reg.train_reg()` | X_train(88维) + y2 |
| T4 (LSTM) | y2 | `deep_lstm.train_lstm()` | X_train(80维) + y2 |
| T1 (XGBoost) | y1 | `tree_cls.train_cls()` | X_train(88维) + y1 |
| T3 (CatBoost) | y3 | `tree_vol.train_vol()` | X_train(88维) + y3 |

### 9.2 预测

```python
T2: pred_t2 = tree_reg.predict_reg(model_t2, X_te)   # (n_stocks,)
T4: pred_t4 = deep_lstm.predict_lstm(model_t4, X_te)   # (n_stocks,)
T1: prob_t1 = tree_cls.predict_cls(model_t1, X_te)      # (n_stocks,) in [0,1]
T3: vol_t3  = tree_vol.predict_vol(model_t3, X_te)       # (n_stocks,)
```

### 9.3 Rank 融合

```python
from scipy.stats import rankdata
rank_t2 = rankdata(-pred_t2, method="average")  # 1=最好
rank_t4 = rankdata(-pred_t4, method="average")
avg_rank = (rank_t2 + rank_t4) / 2.0
selected = np.argsort(avg_rank)[:TOP_N]  # 越小越靠前
```

**为什么用 Rank 融合而不用原始值平均**: 两个模型预测量纲不同。排名归一化后两者平等。举例——T2 预测值范围 [-0.3, +0.3]，T4 范围 [-0.02, +0.02]，直接平均会被 T2 主导。

**自动过滤分歧**: T2 看好（排名第 5）但 T4 不看好（排名第 2500）→ 平均排名 ≈ 1252 → 落选。

### 9.4 T1 方向过滤（可选开关）

```python
if use_t1_filter and t1_recent_auc > 0.58:
    candidates = [s for s in selected if prob_t1[s] > 0.55]
```

阈值 0.55 表示 T1 比随机（50%）至少好 5 个百分点。AUC > 0.58 确保 T1 有预测能力才启用。

### 9.5 市场状态检测 + 大盘趋势判断（可选开关）

**这是风控的核心——在大盘不好时减少买入。** 基于沪深 300 在月末前 20/60/240 个交易日的多维度表现:

```python
class MarketState:
    def detect(month):
        # 1. 短期趋势
        ret_20d = close[-1] / close[-20] - 1      # 20日涨跌幅
        ret_60d = close[-1] / close[-60] - 1      # 60日涨跌幅
        
        # 2. 波动率
        vol_20d  = std(returns[-20:])  * sqrt(252)  # 短期波动率
        vol_60d  = std(returns[-60:])  * sqrt(252)  # 中期波动率
        vol_240d = std(returns[-240:]) * sqrt(252)  # 长期正常波动率
        
        # 3. 回撤
        dd_20d = close[-1] / rolling_max[-20:] - 1
        dd_60d = close[-1] / rolling_max[-60:] - 1
        
        # 4. 均线关系
        ma5_vs_ma20  = ma5 / ma20 - 1     # 短期趋势方向
        ma20_vs_ma60 = ma20 / ma60 - 1    # 中期趋势方向
        
        # 5. 市场广度（上涨股票占比）
        up_ratio = mean(returns[-20:] > 0)
        
        # 综合判断
        if abs(dd_20d) > 0.10:             # 大盘20日跌超10%
            state = "极端熊市"
            exposure = 0.3                   # 仅三成仓位
        elif vol_20d > vol_240d * 2:        # 波动率翻倍（恐慌）
            state = "高波动"
            exposure = 0.5                   # 半仓
        elif ret_20d < -0.05:               # 大盘跌超5%
            state = "熊市"
            exposure = 0.5                   # 半仓
        elif ret_20d < -0.03:               # 大盘小跌
            state = "偏弱"
            exposure = 0.7                   # 七成仓位
        elif up_ratio < 0.35:               # 不足35%股票上涨（二八分化）
            state = "结构分化"
            exposure = 0.7                   # 谨慎
        elif ret_20d > 0.03 and ma5 > ma20: # 大盘涨+均线多头
            state = "牛市"
            exposure = 1.0                   # 满仓
        else:
            state = "震荡"
            exposure = 1.0                   # 满仓

        return {"state": state, "exposure": exposure, ...}
```

**大盘趋势判断的增强**: 相比最初版本只判断「极端/非极端」，新版本增加了 6 个细分状态，对应 5 档仓位（0.3/0.5/0.7/1.0）。在 2026 年 5 月（y2=-42%）这种极端月份，模型会自动降到三成仓位。

### 9.6 仓位调节方法

#### 方法一：T3 波动率调节（已实现）

```python
中性波动率 = 0.25（年化 25%）
weight = clip(0.25 / vol_pred, 0.5, 1.5)
# 高波动(>37.5%) → 降仓至 0.5x
# 低波动(<12.5%) → 加仓至 1.5x
```

#### 方法二：等权分配（默认）

```python
每只股票 budget = 总资金 / TOP_N
# 最简单，无差别对待
```

#### 方法三：IC 加权（新增）

```python
# 根据每只股票在 Rank 融合中的排名位置分配权重
# 排名越靠前，权重越高
rank = avg_rank  # 越小越好
weight = (TOP_N - rank + 1) / sum(1..TOP_N)  # 线性递减
# 第 1 名权重 = TOP_N / sum(1..TOP_N)
# 第 N 名权重 = 1 / sum(1..TOP_N)
```

#### 方法四：最小方差组合（新增）

```python
# 基于历史协方差矩阵，求解最小化组合方差的权重
# 需要至少 60 天的历史收益率数据
cov = covariance_matrix(returns_history[-60:])
weights = 1 / np.diag(cov)  # 简化: 反比于方差
weights = weights / sum(weights)
```

**回测中将对比四种仓位调节方法**: 等权（baseline）、T3 波动率、IC 加权、最小方差。

---

## 十、买入执行

| 参数 | 值 |
|------|-----|
| 执行价格 | 下月第一个交易日 `stock_daily.open` |
| 滑点 | 0.01% → 买入价 = open × 1.0001 |
| 佣金 | 0.025% → 成本 = 买入金额 × 1.00025 |
| 最小单位 | 100 股（整手），不足不买 |
| 单只上限 | 总资金 × 0.9 / TOP_N |

```python
buy_price = open_price * (1 + 0.0001) * (1 + 0.00025)
shares = int(budget / buy_price / 100) * 100
cost = shares * buy_price
```

---

## 十一、持股期间管理（每日执行）

### 11.1 卖出规则（13 条，总分 ≥ 60 触发）

| # | 规则 | 条件 | 分值 | 优先级 |
|---|------|------|------|-------|
| 1 | **硬止损 S1** | 亏损 ≥ 8% | **100** | 穿透 |
| 2 | 移动止盈 T1 | 盈利≥15% 且回落≥8% | 85 | 强 |
| 3 | 持有超时 D1 | >20 天 | 75 | 强 |
| 4 | 死叉确认 M | 连续3天 MA5<MA10 | 70 | 强 |
| 5 | 夏普恶化 SH | 15日夏普<-0.5 | 70 | 强 |
| 6 | 跑输大盘 R1 | 5日跑输>5% | 60 | 中 |
| 7 | 止盈预警 T2 | 回落≥5% | 50 | 中 |
| 8 | 夏普转负 SH | 10日夏普<0 | 50 | 中 |
| 9 | 止损预警 S2 | 亏损≥5% | 40 | 弱 |
| 10 | 即将超时 D2 | >15天 | 40 | 弱 |
| 11 | 死叉初现 M | MA5<MA10 | 40 | 弱 |
| 12 | 跑输预警 R2 | 5日跑输>3% | 30 | 弱 |
| 13 | 夏普偏低 SH | 15日夏普<0.5 | 30 | 弱 |

### 11.2 保护规则

- **T+1 保护**: 买入当日不卖出
- **最低持有**: <5 天不卖（硬止损除外）

### 11.3 V2 因子增强

```python
if prob_t1 < 0.3:    score += 20  # T1 看空加速卖
if excess_ret < -0.03: score += 15  # T2 预期亏损
```

---

## 十二、卖出执行

| 项目 | 费率 | 买方 | 卖方 |
|------|------|:---:|:---:|
| 佣金 | 0.025% | ✅ | ✅ |
| 印花税 | 0.1% | ❌ | ✅ |
| 滑点 | 0.01% | ✅ | ✅ |

```python
sell_price = open_price * (1 - 0.0001)
proceeds = shares * sell_price
cost = proceeds * (0.00025 + 0.001)  # 佣金+印花税
net = proceeds - cost
```

---

## 十三、月末结算

```
1. 卖出全部剩余持仓
2. 本月收益 = Σ(每只股票(卖出净值-买入成本)/买入成本 × weight) / Σweight
3. 扣全部交易成本
4. 记录: 净值、持仓数、现金、收益率
5. 进入下月: 重训练→选股→买入
```

---

## 十四、回测对比矩阵

### 14.1 TOP_N: 10 / 15 / 20 / 25

| TOP_N | 单只资金(等权) | 特点 |
|-------|:----------:|------|
| 10 | 50,000 | 集中，IC 最优区 |
| 15 | 33,333 | 适度分散 |
| 20 | 25,000 | 均衡 |
| 25 | 20,000 | 最大分散 |

### 14.2 时段: 3 个

| 名称 | 范围 | 月数 | 市场 |
|------|------|:--:|------|
| 2025年 | 2025-08~12 | 5 | 温和（y2≈+1%） |
| 2026年 | 2026-01~06 | 6 | 极端（y2 最低-42%） |
| 全周期 | 2025-08~2026-06 | 11 | 混合 |

### 14.3 风控模式: 6 种

| # | 名称 | 说明 |
|---|------|------|
| M0 | 裸融合 | Rank 融合 baseline |
| M1 | +T1过滤 | 方向过滤 |
| M2 | +T3仓位 | 波动率仓位调节 |
| M3 | +大盘风控 | 市场状态检测+趋势判断+降仓 |
| M4 | +IC加权 | IC 排名递减肥仓位 |
| M5 | 全风控 | M1+M2+M3+IC 加权叠加 |

### 14.4 总回测组数

**4 TOP_N × 3 时段 × 6 风控 = 72 组**

（原计划 60 组，新增 IC 加权仓位方法和增强的大盘趋势判断后，扩展为 72 组）

---

## 十五、输出指标

| 指标 | 公式 | 含义 |
|------|------|------|
| 总收益率 | ∏(1+r_i)-1 | 赚了多少 |
| 年化收益率 | (1+总)^(12/n)-1 | 折年化 |
| 夏普比率 | mean(r)/std(r)×√12 | 风险调整收益 |
| 最大回撤 | min(净值/峰值-1) | 最多亏多少 |
| 月胜率 | 正收益月/n | 稳定性 |
| 超额收益 | 策略-基准 | 跑赢多少 |

---

## 十六、结果输出格式

### 16.1 CSV 文件清单

所有结果保存在 `output/backtest_v2/` 下:

| 文件名 | 内容 | 行数 |
|--------|------|:--:|
| `summary_all.csv` | **汇总对比表**——72 组全部指标并排，一屏看清全貌 | 72 |
| `summary_top10.csv` | TOP_N=10 的 18 组详细对比 | 18 |
| `summary_top15.csv` | TOP_N=15 的 18 组详细对比 | 18 |
| `summary_top20.csv` | TOP_N=20 的 18 组详细对比 | 18 |
| `summary_top25.csv` | TOP_N=25 的 18 组详细对比 | 18 |
| `details_M0.csv` | 裸融合——每月持仓、买入价、卖出价、收益率明细 | ~200 |
| `details_M5.csv` | 全风控——每月持仓、买入价、卖出价、收益率明细 | ~200 |
| `monthly_returns.csv` | 72 组 × 11 月 = 792 行的月度收益矩阵 | 792 |
| `optimal_config.json` | 最优配置参数（JSON 格式，供管线读取） | — |

### 16.2 CSV 表头设计

`summary_all.csv` 表头:
```csv
风控模式,TOP_N,时段,月数,总收益率,年化收益率,夏普比率,最大回撤,月胜率,超额收益率,月均换手率,平均持仓天数
```

`details_M0.csv` 表头:
```csv
风控模式,TOP_N,时段,月份,买入股票,买入日期,买入价,卖出日期,卖出价,持仓天数,收益率,累计净值,现金余额
```

---

## 十七、回测结果对比分析

### 17.1 分析维度

回测完成后，从以下维度对 72 组结果进行系统对比:

**维度一: TOP_N 的影响**
- 绘制 TOP_N=10/15/20/25 的四条夏普曲线（横轴=时段，纵轴=夏普）
- 计算各 TOP_N 的平均收益率和标准差
- 判断: 多少只股票时边际收益递减？

**维度二: 风控模式的效果**
- 对比 M0(裸融合) vs M5(全风控) 的净值曲线
- 计算各风控模式降低的最大回撤幅度（%）
- 特别关注: 2026 年 5 月（y=-42%）各风控模式的表现差异

**维度三: 时段适应性**
- 2025 年（正常市场）vs 2026 年（极端市场）的最优配置是否相同？
- 如果不同，说明需要自适应参数切换

**维度四: 费用敏感性**
- 有无交易成本对收益率的影响（回测中默认含费用，可额外跑一组不含费用的对比）
- 月度换手率对组合净值的侵蚀

### 17.2 最优组合选择标准

从 72 组中选出**最优组合**，评分权重:

| 指标 | 权重 | 理由 |
|------|:---:|------|
| 夏普比率 | **40%** | 风险调整后收益，最综合的指标 |
| 最大回撤 | **25%** | 回撤控制比收益更重要（亏 50% 需要赚 100% 回来） |
| 年化收益率 | **20%** | 绝对收益水平 |
| 月胜率 | **15%** | 稳定性——每月都小赚比暴涨暴跌好 |

**综合评分** = 0.4×夏普排序分 + 0.25×回撤排序分 + 0.2×收益排序分 + 0.15×胜率排序分

取综合评分最高的 3 组作为**最终候选**，人工审查其月度明细后确定推荐配置。

### 17.3 对比报告内容

对比分析报告（保存在 `output/backtest_v2/analysis_report.md`）包含:

1. **72 组全景热力图**（行=TOP_N×时段，列=风控模式，颜色=综评）
2. **Top 10 组合详细对比表**（全部指标并排）
3. **最优 3 组月度明细**（每月选股列表、收益、回撤）
4. **最优 vs 最差净值曲线对比图**
5. **2026 年 5 月特写**（最极端月份各策略表现）
6. **小白友好的结论**:「建议使用 ___ + ___ + TOP_N=___，预期年化 ___%，最大回撤 ___%，每月选 ___ 只股票」

---

## 十八、可视化输出

图表保存在 `output/backtest_v2/charts/`:

| 图表 | 数量 | 说明 |
|------|:--:|------|
| 单组净值曲线 | 72 | 策略 vs 基准 + 回撤区间 |
| 全景热力图 | 1 | 72 组综评一览 |
| TOP_N 对比 | 4 | 四种 TOP_N 的夏普曲线 |
| 最优 vs 最差 | 3 | Top 3 vs Bottom 3 的净值对比 |
| 月度收益热力图 | 1 | 72组×11月的收益矩阵 |

---

## 十九、代码模块与实现状态

| 模块 | 路径 | 状态 |
|------|------|:----:|
| 信号融合 | `integration.py` | ✅ |
| 风险控制 | `risk/manager.py` | ⚠️ 需修复前视偏差 + T1 AUC 硬编码 |
| 仓位调节 | `risk/sizer.py` | ⚠️ 需增加 IC 加权和最小方差方法 |
| 卖出规则 | `simulation/rules.py` | ✅ 复用 |
| 卖出配置 | `simulation/config.py` | ✅ 复用 |
| T2 接口 | `models/tree_reg.py` | ✅ |
| T1 接口 | `models/tree_cls.py` | ✅ |
| T3 接口 | `models/tree_vol.py` | ✅ |
| T4 接口 | `models/deep_lstm.py` | ✅ 已修复 search_optuna=False 参数加载 |
| T4 Worker | `scripts/t4_monthly_worker.py` | ✅ **新增**（并行训练，断点续跑，运行时自检） |
| T4 启动器 | `scripts/launch_t4_parallel.sh` | ✅ **新增**（start/status/merge/kill） |
| 80 维缓存 | `data/cache/v2_dataset/9cdff14e8a1a/` | ✅ |
| 88 维缓存 | `data/cache/v2_dataset/d3afaeb9b691/` | ✅ |
| 88 维缓存（最新） | `data/cache/v2_dataset/1e2404aff8dc/` | ✅ 2026-07-30 构建 |
| 市场状态缓存 | 待构建 `market_state_88.npy` | ❌ |
| **回测主程序** | `scripts/run_comprehensive_backtest.py` | ⚠️ **待完善**（风控链路修复） |
| 结果分析脚本 | `scripts/analyze_backtest_results.py` | **❌ 待写** |
| 预测缓存 | `output/backtest_v2/prediction_cache.json` | 🟢 **进行中**（T4 追加） |

---

## 二十、不做什么

- ❌ 不重建数据缓存（全部缓存已就绪）
- ❌ 不使用快速模拟替代真实训练
- ❌ 不使用未来数据（前视偏差零容忍）
- ❌ 不把 T4 改成 88 维（§3.6 已确认 80 维对 LSTM 更优）
- ❌ 不换成日频调仓（月度 Walk-Forward 已验证有效）
- ❌ 不加回 Transformer 层（纯 LSTM 是最优配置）
- ❌ 不在盘中执行数据同步
- ⚠️ ~~不修改已有模块代码~~（大三步阶段已修改：`deep_lstm.py` 参数加载修复 + 线程 `setdefault` 修复, `config.py` 默认值同步）

---

## 二十一、术语表

| 术语 | 含义 |
|------|------|
| IC / Rank IC | 预测排名与实际排名的 Spearman 相关，衡量选股排序质量 |
| y2 | 20 日超额收益（个股收益 - 沪深 300 收益），区间 [-0.5, +0.5] |
| Walk-Forward | 滚动窗口训练→测试，模拟真实交易场景 |
| Fold | Walk-Forward 的一轮（一次训练 + 一次测试） |
| 前视偏差 | 模型训练时「偷看」了未来数据 |
| 夏普比率 | (年化收益 - 无风险利率) / 年化波动率，衡量风险调整后收益 |
| 最大回撤 | 净值从峰值到谷底的最大跌幅 |
| Rank 融合 | 用排名而非原始值的模型集成方法 |
| T1 | XGBoost，预测 5 日涨跌方向 |
| T2 | LightGBM，预测 20 日超额收益 |
| T3 | CatBoost，预测 20 日波动率 |
| T4 | LSTM（纯，不含 Transformer），预测 20 日超额收益 |
| 小三步 | 验证 IC 层面改进的三项实验：月度WF + 88维特征 + T2+T4融合 |
| 大三步 | 验证收益层面的完整链路：T4真实训练 + 72组回测 + 分析报告 |

---


## 二十二、完整验证链条（小三步 → 大三步 → 四项修复）✅ 全部完成

### 三阶段验证

| 阶段 | 日期 | 层面 | 核心结论 |
|------|------|------|---------|
| 小三步 | 7/29 | IC | 月度WF有效, T4=80维, T2+T4融合IC=+0.065 |
| 大三步 | 7/31 | 收益 | T2+T4全周期+55.9%（纯T2:-42.5%） |
| 四项修复 | 7/31 | 风控+权重 | 全周期+89.5%, 夏普3.35, 回撤-12.5% |

### 最终最佳配置

**M4 (+IC加权) + TOP_N=10，全周期11个月**：

| 指标 | 值 |
|------|-----|
| 总收益 | **+89.5%** |
| 年化收益 | **+108.7%** |
| 夏普比率 | **3.35** |
| 最大回撤 | **-12.5%** |
| 月胜率 | 82% (9/11) |
| 终值 | 947,500元（初始500,000） |

### 四项修复效果

| 修复 | 文件:行 | 验证证据 |
|------|---------|---------|
| 市场状态前视偏差 | risk/manager.py:105 | 改用上月最后一天过滤 |
| T1 AUC阈值 | risk/manager.py:221 + monthly_engine.py:804 | T1过滤后: X只 |
| T4动态降权 | integration.py + monthly_engine.py:774 | T4信号弱(std=0.0042), T2权重=0.70 |
| T2分布空仓 | monthly_engine.py:780 | top100均值=-0.1489, 系统性看空→空仓 |

### 修复前后对比

| 指标 | 大三步基线 | 四项修复后 | 改善 |
|------|:-----:|:-----:|:--:|
| 全周期总收益 | +61.3% | **+89.5%** | +28.2% |
| 全周期夏普 | 2.03 | **3.35** | +1.32 |
| 全周期回撤 | -20.6% | **-12.5%** | +8.1% |
| 2026年总收益 | +54.6% | **+75.9%** | +21.3% |
| 2026年夏普 | 2.54 | **5.40** | +2.86 |
| 2026年月胜率 | 83% | **100%** | +17% |
| 2025年总收益 | +3.4% | +3.4% | 持平 |
| 2025年夏普 | 0.59 | **0.70** | +0.11 |

### 2025年分析

2025年总收益+3.4%（纯T2为+10.4%），夏普从0.59提升到0.70。T4在温和市场（y2均值+1.2%）信号强度一般（pred_std~0.02），动态权重未触发降权（阈值0.01），仍部分稀释T2信号。这是"防守换进攻"的结构性取舍——牺牲温和市场7%收益换取极端市场93%改善。

---

## 二十三、后续计划（基于最终结果更新）

### 铁律教训

本次修订中违反铁律三（修复后未逐项验证），导致一次回测跑了旧代码、一次因logger未传入而崩溃。**修改代码后必须做至少一个运行时检查**。

### 建议执行

| # | 改进 | 理由 |
|---|------|------|
| 1 | 2025年T4降权阈值调优 | 当前阈值0.01，可提高到0.02让T4在更多温和月份降权 |
| 2 | 模拟盘接入 | 回测验证通过，M4+TOP_N=10可接入月度生产管线 |
| 3 | 特征提取性能优化 | _extract_per_day_features对全历史计算，仅需最近400行 |

### 长期探索

- T2+T4动态权重用真实IC（需要逐月计算）替代pred_std启发式
- 训练窗口自适应
- 月度→双周调仓对比

### 始终坚持不做

- T4改88维（3.6已证伪）
- 日频调仓（成本>收益）
- 加Transformer层（1.2已证伪）

---

## 二十四、数据扩展与缓存重建记录（2026-07-31 ~ 08-01）

### 24.1 背景

原数据仅覆盖 2024-01 ~ 2026-07（2.5 年，624 交易日），回测仅 11 个月。为扩展回测周期到 2020-01 起，执行历史数据补齐。

### 24.2 历史数据补齐（2026-07-31 晚，三次迭代）

| 迭代 | 方法 | 结果 | 失败原因 |
|------|------|------|---------|
| v1 | `sync_daily(force=True)` + start_date=2020 | filled=0 | force 模式对已有数据的股票从 last_date 开始，不拉历史 |
| v2 | `backfill()` baostock 逐只 | 太慢（2.2s/只 × 5203 ≈ 3h）且 baostock 不稳 | 接口速度限制 |
| v3 | 直接调 `TencentSource.get_daily(code, days=1600)` | ✅ 成功 | — |

**最终结果**：

| 指标 | 补齐前 | 补齐后 |
|------|:---:|:---:|
| 日期范围 | 2024-01-02 ~ 2026-07-31 | **2020-01-02 ~ 2026-07-31** |
| 交易日 | 624 | **1594** |
| 总记录 | 317 万 | **736 万** |
| 指数（6 只） | 624 天 | **1594 天** |
| peTTM 有值 | 235 万 (72%) | **536 万 (73%，逐年稳定)** |
| pbMRQ 有值 | 316 万 | **729 万 (99.1%)** |

**数据源**：OHLCV = Tencent → Sina → baostock 三轨（Tencent 主力）；估值 = TDX → baostock；指数 = Tencent。

**空值处理**：
- peTTM/pbMRQ：forward-fill（每股按日期用前值填充）+ backward-fill 开头
- OHLC：3 条空值已填
- volume：171 条空值已填（⚠️ 教训：volume=0 是停牌日正确值，不应填——错填后用 API 重拉 64 只修复部分）

**⚠️ 重要发现：volume 单位混用（2026-08-02 实测已统一，更新）**
- Tencent fqkline 返回**手**（lots），baostock/Sina 返回**股**（shares）
- 已修复源头：`tencent_source.py::_tencent_kline` 加 `volume * 100` 转换（手→股，2026-07-31 22:12 mtime）
- **✅ 存量实测（2026-08-02）**：
  - 全量扫描 4956 只（2020-2023 vs 2024-2026.6 前/后段 volume 中位数比值）：仅 1 只疑似（300442，比值 26——经逐年核查系 2023 年题材炒作正常放量，非单位混入）
  - 100 只随机抽查（三段：2020-2023 / 2024-2026.6 / 2026.7 后 Tencent 同步段）：**0 只级数变化**（相邻段比值 0.4~11.9，全部正常）
  - 样本验证（600519 茅台三段）：297 万 / 314 万 / 476 万股——量级连续无 100 倍跳变
- **结论：当前数据库成交量单位统一为"股"**，前中后段一致，**无需修复存量**（此前记载的"~500 只混入手"系 7/31 晚中间状态，后续修复/重拉已纠正）

### 24.3 缓存重建（2026-08-01）

数据扩展后必须重建全部缓存（采样日从 48 天 → 144 天）。

**代码改动**：
- `features.py`：`_extract_per_day_features` / `build_stock_features` / `build_prediction_features` 新增 `include_market_state` 参数（T4=80维传 False，树模型=88维传 True）
- `labels.py`：`_process_chunk` / `_dataset_cache_path` / `build_training_dataset` 透传该参数；缓存 key 增加 `market_state` 字段

**新缓存**（16 workers，总耗时 215.6 min）：

| 缓存 Hash | 维度 | 样本数 | 采样日 | 用途 |
|-----------|:---:|:---:|:---:|------|
| `62cf234c5440` | **80维** | 400,728 | 144 (2020-08~2026-07) | T4 LSTM |
| `13132147f8e8` | **88维** | 400,728 | 144 | T2/T1/T3 |
| ~~9cdff14e8a1a~~ | 80维 | 135,795 | 48 | 旧，可删 |
| ~~1e2404aff8dc~~ | 88维 | 135,945 | 48 | 旧，可删 |

**路径更新**：`t4_monthly_worker.py` → `62cf234c5440`；`build_prediction_cache.py` → `13132147f8e8`

### 24.4 60 个月预测缓存与 T4 训练（进行中）

**目标**：回测周期从 11 个月扩展到 60 个月（2020-09 ~ 2026-06，2020-08 因 12 月窗口数据不足跳过）。

**T2/T1/T3 预测缓存**（`build_prediction_cache.py --start-month 2020-08 --end-month 2026-06 --skip-t4`）：
- 8 进程并行，每月份独立 Worker：T2 训练 → T1 训练 → T3 训练 → OHLCV 特征 → 预测
- 速率约 5.6 月/小时，71 个月总耗时约 12-15h
- ⚠️ **数据一致性修复**：旧 11 个月（2025-08~2026-06）用旧缓存（13.5 万样本）训练，与新月不一致 → 已从缓存删除（备份 `prediction_cache_backup_20260801_1804.json`），待补建

**T4 71 个月训练**（备用就绪）：
- `t4_monthly_worker.py`：`ALL_MONTHS` 动态生成 71 个月（2020-08 ~ 2026-06）
- `launch_t4_parallel.sh`：`MAX_PARALLEL=8` 并发控制（36 核 ÷ 5 线程/进程）
- 预计 ~20h（8 并发 × 2.3h/批 × 9 批）

**自动管线**（`pipeline_t4_auto_start.sh`，三阶段）：
```
阶段1: 等待 T2/T1/T3 主构建退出（处理 2023-01~2025-07）
阶段2: 补建 2025-08~2026-06 的 T2/T1/T3（11 个月）
阶段3: 确认 2026-06 完成 → 自动启动 T4 71 个月
```

**踩坑记录**：
- 监督脚本误触发：旧缓存含 2026-06 → T4 提前启动 → 已修正检查逻辑 + 停止
- bash 内联 python 引号嵌套语法错误 → 改用 heredoc `<< 'PYEOF'`

### 24.5 后续

1. T4 71 个月训练完成后 → 扩展回测到 60 个月
2. 60 个月回测 → 数据统计显著性大幅提升，重新评估最优配置
3. 可选：API 空闲时修复 ~500 只 volume"手"混入股票

---

## 二十五、2025 年收益率提升优化方案（2026-08-01 讨论记录）

### 25.1 问题背景

最终配置 M4 + TOP_N=10 全周期 +89.5%/夏普 3.35，但 **2025 年 5 个月仅 +3.4%（夏普 0.70）**，vs 纯 T2 的 +10.4%。2026 年 +75.9% 与 2025 年形成巨大反差。

### 25.2 根因分析（2026-08-02 用 60 个月数据修正）

**原判断（部分正确）**：T4 在温和市场稀释 T2 信号，pred_std 启发式降权失效：

- 当前降权机制（`backtest/monthly_engine.py`）：`w_t4 = 0.25 + 0.25 * min(t4_std/0.02, 1.0)`——用预测离散度当"信号质量"
- **缺陷**：pred_std（预测值分散程度）≠ 预测质量。2025 年 T4 预测分散但排得不准 → 权重未降 → 稀释 T2。

**60 个月逐月 IC 分析修正（关键新发现）**：2025 年**两个模型 IC 本身都接近 0**（T2 -0.005 / T4 -0.006，全年 12 个月均值）——2025 收益低的根因**不只是融合稀释，模型排序能力本身弱**。详见 §25.6。

**结构性补充**：T2/T4 优势年份分化明显（2021-22/24 T4 强、2023/26 T2 强），**"温和市 T2 强、极端市 T4 强"假设被 60 个月数据推翻**（实际：熊市 T2 强 +0.058、牛市/温和涨 T4 强）→ 动态权重**不能依赖市场状态假设，应基于滚动真实 IC**（§25.3 方案 1）。

### 25.3 优化方案（按影响排序）

#### 🥇 方案 1：滚动 IC 动态加权（首选，替代 pred_std 启发式）

- **思路**：用过去 6 个月 T2/T4 的真实月度 Rank IC 均值决定本月融合权重：
  ```python
  ic_t2 = mean(rolling_ic_t2[-6:]);  ic_t4 = mean(rolling_ic_t4[-6:])
  w_t2 = clip(ic_t2 / (ic_t2 + ic_t4), 0.3, 0.7)
  ```
- **代码已就绪**：`integration.py:77 rank_weighted_fusion()`（含 ic_t2/ic_t4 参数 + min/max_weight，docstring 即"解决 T4 在温和市场稀释 T2 信号"）——**只差接入 monthly_engine.py**
- **预期**：2025 年 T2 权重自动升至 ~0.7 → 收益向 +10% 靠拢；2026 年极端月 T4 IC 高时自动给权重，保留防守——"防守换进攻"从固定取舍变自适应
- **风险**：滚动 IC 对拐点滞后（memory/model-integration-plan 已记载）；6 月窗口 + [0.3,0.7] 上下限缓解
- 升级替代 §23 建议 #1（pred_std 阈值 0.01→0.02 的粗放调优）

#### 🥈 方案 2：月度归因 + 分散约束（修具体拖累月）

- 2025 年 5 个月 4 正 1 负，**2025-08 的 -5.6% 是主要拖累**
- 约束：单只权重上限（IC 加权下第一名可能过重）、行业集中度 ≤30%（需行业数据）
- 或温和市用 TOP_N=15（小三步：2025 年 TOP_N=15 时 +16.5% > TOP_N=10 的 +10.4%）

#### 🥉 方案 3：TOP_N 自适应

- 小三步：2025 年 TOP_N=20 纯 T2 +25.8%，融合 TOP_N=10 最优 → 温和市分散更优、极端市集中更优
- 用滚动收益/波动率自适应切换 TOP_N；复杂度高，等 60 个月回测全貌再定

#### 方案 4：T2 强化（提升所有市场基础能力）

- Optuna trials 50→100（2h 超时内）；T2 特征扩展（量价背离、资金流代理）
- 边际有限（IC +0.094 已不错），不依赖市场状态

#### 方案 5：成本优化

- 2025 年 5 个月 +3.4% 中交易成本侵蚀不小（买卖 ~0.16%+ × 10 只 × 月度调仓）
- 检查月均换手率：若 D1 超时（20 天）导致频繁换手 → 延长持有可省成本

### 25.4 落地路径（2026-08-02 更新：方案 1 被 70 个月数据证伪）

1. ✅ **60 个月逐月 T2/T4 Rank IC 分析已完成**（结果见 §25.6）——假设被推翻（熊市 T2 强、牛市/温和涨 T4 强）
2. ✅ **方案 1 已实施并验证**：`monthly_engine.py` 新增 `fusion_method` 参数（`ic_weighted` = 滚动 6 月 IC 加权，权重 [0.3,0.7]，IC<0 取 0）；回测脚本 `--fusion-method ic_weighted`
3. ❌ **方案 1 验证结论（70 个月全量，M0 TOP_N=10）——被证伪**：
   | 指标 | pred_std | ic_weighted |
   |------|:-------:|:----------:|
   | 总收益 | **+757.38%** | +693.37% |
   | 夏普 | **2.33** | 2.22 |
   | 回撤 | **-17.29%** | -19.56% |
   **失败原因**：① 滚动 IC 预测性不足——T2/T4 优势年份切换频繁（2021-22 T4 强、2023/26 T2 强），6 月滚动窗口在风格切换点"追错人"；② IC<0 取 0 在 IC≈0 的月份（2025）使权重极端化；③ pred_std 启发式虽不科学但稳健（T4 预测坍缩 = 其真实失效模式，启发式恰好抓住）。11 个月小样本"略好"为噪声。
4. ✅ **默认融合保留 pred_std**；`ic_weighted` 代码保留（`--fusion-method` 参数可切换，存档备查）
5. **2025 年优化转向方案 2（分散约束）+ 方案 4（T2 强化）**——§25.6 确认 2025 年两模型 IC≈0，调权重空间有限
6. 🔄 **72 组全量回测以 pred_std 运行**（70 个月 × 4 TOP_N × 3 时段 × 6 风控，24 进程并行）

### 25.5 注意事项

- 5 个月样本有统计噪声，+3.4% vs +10.4% 差异未必全部来自权重问题——60 个月全貌先看
- 本方案不改变 §二十 的"不做"清单（T4 改 88 维 / 日频调仓 / 加 Transformer 仍不做）

### 25.6 60 个月逐月 Rank IC 分析结果（2026-08-02 完成）

**方法**：prediction_cache 70 个月 × 全部股票（~2900 只）Spearman（预测 vs 实际 20 日超额收益，沪深 300 基准）。
**完整数据**：`output/backtest_v2/monthly_ic_analysis.csv`；分析脚本 `scripts/analyze_monthly_ic.py`。

#### 按年度汇总

| 年份 | 月数 | T2 IC | T4 IC | 胜者 | y2 均值 |
|------|:---:|:-----:|:-----:|:----:|:------:|
| 2020 | 4 | +0.041 | +0.070 | T4 | -4.6% |
| 2021 | 12 | +0.002 | +0.015 | T4 | +2.9% |
| 2022 | 12 | +0.020 | +0.033 | T4 | +2.2% |
| 2023 | 12 | +0.026 | +0.018 | T2 | +0.7% |
| 2024 | 12 | -0.001 | +0.019 | T4 | -0.6% |
| 2025 | 12 | **-0.005** | **-0.006** | 平 | +1.5% |
| 2026 | 6 | +0.012 | -0.016 | T2 | -2.6% |

#### 按市场状态汇总（验证"温和市 T2 强、极端市 T4 强"假设）

| 市场状态 | 月数 | T2 IC | T4 IC | 结论 |
|----------|:---:|:-----:|:-----:|------|
| 温和涨 | 22 | **-0.026** | +0.000 | T4 优 |
| 温和跌 | 23 | +0.031 | +0.034 | 持平 |
| **熊市** | 13 | **+0.058** | +0.017 | **T2 显著强** |
| 牛市 | 12 | -0.014 | +0.012 | T4 优 |

**假设被推翻**：熊市（极端市）反而 T2 强；牛市/温和涨 T4 强。原"温和市 T2 强、极端市 T4 强"不成立。

#### 核心发现

1. **2025 年两模型 IC 均 ≈ 0**（T2 -0.005 / T4 -0.006）——2025 收益低的根因是**模型排序能力本身弱**（不只是融合稀释）
2. **T2/T4 优势年份分化**：2021-22/24 T4 强、2023/26 T2 强 → **滚动真实 IC 加权有明确空间**（不依赖市场状态假设）
3. 熊市 T2 IC +0.058 最高 → 2026 年高收益主要贡献者为 T2（回测 M4 高收益机制需重新审视）
4. 全部年份 IC 均值 0.00~0.05 区间——**单模型排序能力整体偏弱**，融合与风控的价值被低估

#### 对方案的影响

- **方案 1（滚动 IC 加权）成立且已实施**（§25.4）——依据是年份分化的实际 IC，非市场状态假设
- **2025 年需组合方案 2/4**（分散约束 + T2 强化）——仅调权重空间有限

---

## 二十六、多进程 worker 满核运行铁律——KMP_AFFINITY 绑核教训（2026-08-01）

> **一句话教训**：`.bashrc` 的 `KMP_AFFINITY` 会让所有多进程 worker 抢同一个核心——进程"活着"但 CPU 利用率 1 核，训练全卡。排查 4 小时，浪费一整轮 build 运行。

### 26.1 事故经过

2026-08-01 晚，60 个月扩展回测流水线：

| 版本 | 配置 | 现象 | 排查结论 |
|------|------|------|---------|
| v1 (08:08) | 8 worker × OMP=4 | ✅ 正常（24 月/11h） | 8×4=32 线程恰好错开绑定 |
| v2 (19:35) | 16 worker × OMP=4 | ❌ 卡 Step3 (T3) | 误判"线程风暴"，改 OMP=1 |
| v3-v6 (20:30) | 24 worker × OMP=1 | ❌ 全卡 Step1，CPU 1 核 | 误判"BLAS 线程"/"fork 死锁" |
| **v7 (21:05)** | 24 worker + **KMP 清除** | ✅✅ CPU 87%，worker 各 96-99% | **真凶：KMP_AFFINITY 绑核** |

**浪费的弯路**：改 OMP 4→1、n_jobs 4→1、线程环境变量硬赋值、单 worker 最小复现（99.6% CPU 正常）——全部不是根因。单 worker 正常正是迷惑点：它被绑到单核但无竞争者，看起来"正常计算"。

### 26.2 根因链

```
.bashrc: KMP_AFFINITY=granularity=fine,verbose,compact,1,0
    → OpenMP 库把每个进程的线程按 compact 模式绑定
    → 所有 worker 的主线程都绑定到"从基础核开始的连续子集"
    → 24 个 worker 全部挤在同一核心（proc set 0）
    → 24 进程互相抢占 1 个核 → CPU 利用率 1 核假象 → 训练寸步难行
```

为什么 8 worker 正常？8 进程 × 4 线程的紧凑绑定恰好分散到 32 个核心。**进程数 × 线程数 > 核数时，compact 绑定必然重叠**。

### 26.3 诊断标志（5 分钟内定位）

| 标志 | 命令 | 判定 |
|------|------|------|
| 日志尾部出现 `OMP: Info #255: KMP_AFFINITY: ... bound to OS proc set` | `tail -30 worker.log` | ✅ 确诊 |
| 总 CPU 低（<15%）但进程数多 | `top -bn1 \| grep %Cpu` | 疑似 |
| 进程 State=R 但 CPU 极低 | `cat /proc/PID/wchan`（返回 0）+ `ps -o pcpu` | 疑似（用户态自旋） |
| nonvoluntary_ctxt_switches 数千次 | `grep nonvoluntary /proc/PID/status` | 印证（被强制抢占） |
| 单 worker 测试正常（99%+） | 独立进程跑同任务 | 迷惑点：不排除绑核 |

### 26.4 修复模板（适用于 T4 / build / 回测所有多进程程序）

```python
# ① Worker 脚本模块级——必须在 import numpy/tensorflow/任何数值库之前
os.environ.pop("KMP_AFFINITY", None)          # 必须 pop（setdefault 对 .bashrc 已存在的值无效！）
os.environ["OMP_NUM_THREADS"] = "1"           # 硬赋值（setdefault 无效，.bashrc 有 OMP_NUM_THREADS=36）
os.environ["OPENBLAS_NUM_THREADS"] = "1"
os.environ["MKL_NUM_THREADS"] = "1"
os.environ["NUMEXPR_NUM_THREADS"] = "1"
```

```bash
# ② 启动命令双保险
env -u KMP_AFFINITY -u OMP_NUM_THREADS nohup python -u worker.py > logs/x.log 2>&1 &
```

**线程预算**（36 核）：`worker 数 × OMP(1) ≤ 36`。训练用框架自身并行参数（如 LightGBM `n_jobs`），BLAS/OMP 一律 1。

### 26.5 已验证的修复位置

| 文件 | 修复 |
|------|------|
| `scripts/t4_monthly_worker.py` (~L22) | `os.environ.pop("KMP_AFFINITY", None)` + TF 2/1 + OMP=2 |
| `scripts/launch_t4_parallel.sh` (launch) | `env -u KMP_AFFINITY -u OMP_NUM_THREADS` 启动 worker |
| `scripts/build_prediction_cache.py` (顶部) | `pop("KMP_AFFINITY")` + OMP/OPENBLAS/MKL/NUMEXPR 硬设 1 |
| `scripts/build_prediction_cache.py` (worker 内) | 同样四件套 + 启动诊断打印 `KMP清除` |

### 26.6 验证方法（铁律三）

修复后 5 分钟内验证：
1. `grep -c KMP_AFFINITY <worker_log>` → 必须 = 0
2. `top -bn2 -d 3 | grep %Cpu` → 总 CPU ≥ 85%（24 build + 12 T4 满负荷）
3. worker 逐个 `ps -o pcpu` → 各 90%+（之前 5.9%）
4. 阶段日志推进（Step1→2→3→5）→ 之前卡 Step1 不动

---

## 二十七、60 个月扩展回测最终结论（2026-08-02）

### 27.1 数据与执行

- **周期**：2020-09 ~ 2026-06（70 个月，全市场 ~2900 只）
- **组数**：72 组 = 4 TOP_N × 3 时段 × 6 风控
- **时段**：全周期(70月) / 2025年(12月) / 2026年(6月)
- **并行**：24 进程（`scripts/launch_backtest_parallel.sh`），72 组 ~7 分钟
- **产出**：`output/backtest_v2/summary_all_60m.csv`、`analysis_report.md`、`details_*.json`

### 27.2 最终最优配置

**M4（+IC加权仓位）+ TOP_N=10，全周期 70 个月（py312 数据）**：

| 指标 | 值 |
|------|-----|
| 总收益 | **+944.8%**（50 万 → 522 万，py312） |
| 年化 | +52.1% |
| 夏普 | 2.28 |
| 最大回撤 | -18.2% |
| 月胜率 | 70% |
| 综合评分 | 0.836（72 组第一） |

**三时段最优**：

| 时段 | 最优配置 | 收益 | 备注 |
|------|---------|-----:|------|
| 2025 年 | M5 + TOP_N=10 | **+25.8%** | 全年不低！旧结论（+3.4%）仅是 8-12 月窗口 |
| 2026 年 | M5 + TOP_N=10 | **+90.9%** | 年化 +307%，夏普 4.28 |
| 全周期 | M4 + TOP_N=10 | +944.8% | 综合评分 0.836 |

### 27.3 三大发现（60 个月扩展的价值）

1. **"2025 年收益低"问题大幅缓解**：2025 年全年 +25.8%（夏普 1.74）——之前的 +3.4% 只是 2025-08~12 的 5 个月窗口，**2025 上半年贡献显著**。数据扩展直接修正旧结论
2. **M4 + TOP_N=10 结论稳健**：11 个月与 70 个月的最优配置一致（TOP_N=25 夏普最高 2.51，但综合评分 M4+10 胜出——收益+胜率更平衡）
3. **长周期量级可信**：+932%（年化 51.8%），跨 2020-2026 牛熊周期，比 11 个月结论可信得多

### 27.4 遗留观察项

1. ✅ **M0 = M1 已核查并正式关闭 T1（2026-08-02）**：
   - 70 个月真实 AUC 均值 0.4987（仅 2/70 月 >0.58 阈值）
   - **方向可学性实证**（`scripts/quick_t1_experiment.py`，70 个月）：换目标全部 ≈0.5——20 日超额方向（T2 回归方向化）AUC=0.5059、10 日方向 0.4993、20 日方向 0.4975。**结论：A 股短期方向在 88 维日线特征下本质不可学**（T2 回归能提取微弱连续信号 IC≈0.03，离散化后消失）——不是目标/模型问题，是任务本质
   - **处置**：T1 方向过滤从风控体系移除（`monthly_engine.py` auc 已修正为 0.5 保持关闭）；风控体系实际 = M2（T3 波动率仓位）+ M3（大盘状态）+ M4（IC 加权仓位），三者已实证有效
   - 若未来要方向风控，需换特征体系（分钟级/资金流），属项目级新话题
2. **TOP_N=25 夏普 2.51 > M4+10 的 2.27**：风险偏好权衡——稳健型可考虑 TOP_N=15/25
3. **2025-08~12 弱月**：全年 OK 但下半年偏弱，可做归因分析（§25 方案 2 分散约束仍可选）
4. **方案 1（滚动 IC 加权）已证伪**（§25.4）——默认保留 pred_std 融合

## 二十八、月度重训与预测机制详解（2026-08-02 澄清记录）

> 本节回答"训练、预测、选股的原理、流程和逻辑"——特别是**训练数据与预测特征的区别**，
> 以及**为什么某些近期数据不能用于训练（防未来数据的本质）**。

### 28.1 完整时序（以 8 月重训为例）

```
7/31 周五    ← 7 月最后交易日：数据同步完成（数据库含 ≤7/31 全部数据）
8/1 周六     ← 训练（用"答案已揭晓"的历史样本）+ 预测（特征用截至 7/31 的完整历史）
8/2 周日     ← 选股完成（TOP10 名单入库 sim_v2.db）
8/3 周一     ← 8 月第一个交易日：晚上执行买入（当天开盘价，收盘价算当日收益）
```

**关键原则：8 月选股 = 站在 8/1 时点（知道 7/31 及以前的一切），选出 8 月最可能跑赢大盘的股票。**
能用的信息极限就是 7/31——8 月数据要到 8 月才产生，不存在"用 8 月数据选 8 月股"（那才是未来数据）。

### 28.2 训练集：采样日机制 + 标签"答案揭晓"机制

**采样日**（`labels.py _get_sample_dates`）：每月取 2 个交易日（第 5 个、第 15 个）作为采样点。
每个采样日 = 全市场 ~2900 只股票各一条训练样本（120 天窗口 × 88 维特征 + 标签）。

**标签（y2）定义**：未来 20 个交易日的超额收益（个股 20 日收益 - 沪深300 同期）。

**关键机制——标签像"考试答案"，要等 20 天后才公布**：

| 采样日 | y2 答案公布日（+20 交易日） | 8/1 训练时可用？ |
|--------|---------------------------|:---:|
| 2026-06-22 | ~7/21（7 月数据已同步） | ✅ 答案已公布，可用 |
| 2026-07-07 | ~8/4（需 8 月价格） | ❌ 答案未公布，不可用 |
| 2026-07-21 | ~8/18（需 8 月价格） | ❌ 答案未公布，不可用 |

**因此：8 月重训的训练集 = 2025-08 ~ 2026-06 的采样日（142 个，答案全部已揭晓）**。
7 月的 2 个采样日被排除**不是因为"用了 8 月数据"，恰恰相反——因为它们的答案需要 8 月数据才可知，
8/1 训练时答案未揭晓，用了才是"未来数据作弊"**。这 2 个样本在 9 月重训时自然可用（届时答案公布）。

### 28.3 训练 vs 预测的区别（核心概念）

| 环节 | 数据 | 逻辑 |
|------|------|------|
| **训练** | 历史采样日样本（答案已揭晓） | 学规律：什么样的特征组合 → 未来 20 日跑赢 |
| **预测** | **截至 ref_date 的完整历史画面** | 用最新数据 + 学到的规律 → 判断未来 |
| **买入** | 执行日开盘价 | T+1 模型，收盘价算当日收益 |

**预测特征不是"ref_date 单日数据"，而是"截至 ref_date 的最近 120 个交易日完整历史"**：
- LSTM 主输入：120 天 × 88 维时序窗口（如 2026-02 ~ 7/31，**7 月全月 23 天都在窗口内**）
- T2 长周期指标：MA60/MA120、60 日分位、240 日波动率等（用到更长期历史）
- "7/31"是数据**截止日期**（ref_date），不是只取这一天——7 月的每一天（涨跌/量能/形态）都作为特征输入

### 28.4 类比（小白友好）

**预测"明天谁考试得高分"**：
- 训练：用过去 11 个月所有**已出分**的考试成绩（答案已揭晓的历史样本）
- 预测：用**今天**的复习状态、模拟考成绩（最新历史画面）打分
- 买入：明天执行（结果明天才知道）

### 28.5 月度滚动循环

```
每月底数据完整 → 下月 1 日 00:00 重训（训练集自动延伸：上上月样本答案已揭晓）
  → 用最新 ref_date（上月末）特征预测 → 选股入库
  → 下月第一个交易日晚上执行买入 → 月末清仓 → 循环
```

每月重训的意义：训练集永远用"答案已揭晓"的最新样本 + 预测特征永远用"最新的历史画面"——**既无未来数据，又不浪费最新信息**。

### 28.6 常见误解澄清（Q&A）

| 误解 | 澄清 |
|------|------|
| "预测只喂 7/31 一天的数据" | ❌ 喂的是截至 7/31 的 120 天完整历史（7 月全月都在） |
| "6/22 同步完就能预测 8 月选股" | ❌ 可以训练，但预测特征缺 7 月全月信息 → 选股不准（必须等 7/31） |
| "用 2025-07~2026-06 历史预测 8 月" | ✅ 训练用历史（答案已揭晓）✓，但**预测特征必须用 7/31 最新** |
| "缓存没有 7 月样本是缺陷" | ❌ 7 月样本的答案（y2）8 月初未公布，本来就不能用——是防未来数据的正确行为 |

### 28.7 实现位置

| 环节 | 代码 |
|------|------|
| 采样日生成 | `labels.py _get_sample_dates`（每月第 5/15 交易日） |
| 标签计算 | `labels.py _compute_label_t1/t2/t3`（y2 = 未来 20 日超额，需未来价格） |
| 特征构建 | `features.py`（ref_date 过滤：`df[df["date"] <= ref_date]`） |
| 月度重训 | `scripts/v2_monthly_retrain.py`（build 缓存 → T4 → 选股 → 入库） |
| 执行买入 | `scripts/v2_simulation_daily.py`（T+1 模型） |

