# Sequoia-X V2 框架完整指南（V3.0）

> **版本**: V3.0（全面修订版） | **最后更新**: 2026-08-02 | **维护者**: zhulei
>
> 本文档是 Sequoia-X V2 多模型 A 股月度选股系统的**完整框架指南**，涵盖 V2 架构的背景、起源、
> 分析、优化、完善、验证、训练、回测、模拟盘运行、失误、不足与后续优化方向的全过程信息。
> **后续只需阅读本指南即可全面理解 V2 框架。**
>
> **配套文档**：`BACKTEST_PLAN.md`（开发计划书/技术细节）、`BACKTEST_GUIDE.md`（回测结果详解/小白版）、
> `CLAUDE.md`（工作指南）、`STATUSLINE_TUNING.md`（状态栏调优）、`模拟盘交易模块使用说明.md`。
> **旧版指南**（V2.0，2026-07-24）已备份为 `V2_OPERATION_GUIDE_v2_backup.md`。

---

# 第一部分：架构背景与起源

---

## 1. 项目全景与 V2 的起源

### 1.1 项目定位

Sequoia-X 是一套个人量化交易研究系统，由多个独立项目组成。本项目（`017_workbuddy/004_sequoia-x`）
专注于**多模型股票排序**——不预测股价涨跌的绝对数值，而是对全市场股票进行"谁更可能跑赢大盘"的排序。
这类似考试排名而非预测分数：不需要精确知道每只股票下月涨多少，只需要知道**哪几只股票最可能涨得比别的股票多**。

**V1（`model_selection`）**：单个 LSTM-Transformer 深度模型，直接预测股票未来收益率。
**V2（`model_selection_v2`）**：引入四模型体系（树模型 + 深度模型互补），增加 Walk-Forward 验证框架，
最终形成"T2(88维)+T4(80维) Rank 融合 + 月度重训 + 月度调仓"的生产体系。

### 1.2 V2 的核心目标

每月从全市场 ~2900 只 A 股中选出**未来 20 个交易日最可能跑赢沪深 300 的 10~25 只股票**买入，月底换仓。
"20 日超额收益"是选股目标——不预测涨跌绝对值（大盘涨大家都涨），只预测"谁比大盘涨得多"。

### 1.3 系统演进时间线

| 时间 | 事件 |
|------|------|
| 2026-07 前 | V1 LSTM-Transformer 体系（季/周/天多周期，50 万模拟盘） |
| 2026-07-22~28 | V2 四模型体系建立，T4 LSTM 常数预测四轮调试（见 §9） |
| 2026-07-29 | 小三步验证（月度 WF / 88 维 / 融合）；大三步验证启动 |
| 2026-07-30 | BACKTEST_PLAN 综合回测计划书；72 组回测框架 |
| 2026-07-31 | 大三步完成（T2+T4 全周期 +89.5%，四项修复）；数据扩展至 2020-01 |
| 2026-08-01 | 60 个月扩展回测流水线（KMP 锁核修复、并发调优、T4/build 并行时序修复） |
| 2026-08-02 | 70/71 个月全量回测完成（py312 最终口径）；8 月提前重训；模拟盘代码落地；环境一致性铁律 |

---

## 2. 四模型体系设计

### 2.1 为什么是四个模型（医生类比）

| 模型 | 类型 | 预测目标 | 角色类比 | 状态 |
|------|------|---------|---------|:----:|
| T1 | XGBoost 分类器 | 5 日涨跌方向（y1） | 初步筛查 | ❌ 已关闭（实证无能力，见 §8.5） |
| **T2** | LightGBM 回归器 | 20 日超额收益（y2） | **主力医生** | ✅ |
| T3 | CatBoost 回归器 | 20 日波动率（y3） | 风险评估 | ✅ |
| **T4** | LSTM 神经网络 | 20 日超额收益（y2） | **专家会诊** | ✅ |

**树模型（T1/T2/T3）** 擅长发现"如果 PE 低于 15 且成交量放大 50%，大概率跑赢大盘"这样的**条件规则**
（看截面：同一时间点不同股票的比较）。
**LSTM（T4）** 擅长发现"PE 在过去 20 天加速下降 + 量能萎缩后突然爆发"这样的**时序模式**
（看时序：同一股票的历史演变）。
两者互补——这是"双主力"设计的根本依据。

### 2.2 预测目标详解

**y1 — 5 日方向（二分类）**：5 个交易日后股价 > 今日股价 → 1，否则 0。T1 预测（已停用）。

**y2 — 20 日超额收益（回归，核心目标）**：
```
stock_ret = (20天后股价 / 今天股价) - 1
idx_ret   = (20天后沪深300 / 今天沪深300) - 1
y2 = clip(stock_ret - idx_ret, -0.5, +0.5)
```
裁剪到 [-0.5, +0.5] 防止极端值干扰。T2 和 T4 预测此目标。

**y3 — 20 日年化波动率（回归）**：未来 20 个交易日每日收益率的标准差 × √252。T3 预测此目标。

### 2.3 特征体系（80 维 → 88 维）

特征分组（80 维基础版 + 8 维市场状态 = 88 维）：

| 组号 | 类别 | 维度 | 包含指标 |
|------|------|:----:|---------|
| 1 | 价格收益 | 8 | ret_1d/5d/10d/20d、跳空缺口、振幅、涨跌幅、5日振幅 |
| 2 | 均线偏离 | 6 | MA5/10/20/60/120 偏离度 + MA5/MA20 比值 |
| 3 | 量能 | 8 | 量比、5日量变化、换手率、成交额比、量价相关、量能趋势、放量标记 |
| 4 | 技术指标 | 11 | RSI、MACD(DIF/DEA/柱)、布林带、ATR、KDJ、OBV变化、ADX |
| 5 | 波动率 | 4 | 5/10/20 日年化波动率 + 短/长波动率比值 |
| 6 | 大盘关联 | 8 | Beta、20日超额收益、指数偏离等 |
| **6b** | **市场状态(新增)** | **8** | 大盘20/60日涨跌幅、波动率、波动加速度、回撤、均线、上涨占比 |
| 7 | 价格形态 | 7 | 涨跌停、连涨/连跌、新高/新低、K线实体比 |
| 7b | 最大回撤 | 3 | 20/60日回撤、恢复率 |
| 7c | 收益分布 | 4 | 偏度、峰度、上涨天数比、非对称波动 |
| 7d | 时间日历 | 4 | 周几(sin/cos编码)、月几日、季末标记 |
| 7e | 价格位置 | 3 | 收盘位置、5日平均跳空、短/长振幅比 |
| 8 | 估值 | 4 | peTTM、pbMRQ + 各自60日分位 |

**特征工程严格规则——杜绝前视偏差**：第 T 日的特征**只能使用第 T 日及之前已知的数据**。
归一化方式是**每只股票独立、沿时间轴做 Z-score**（`features.py`：`df = df[df["date"] <= ref_date]` 过滤）。

### 2.4 非对称特征设计（88 维 vs 80 维）

§3.6 实验证明：**LSTM 能从 120 步时序中隐式推断市场状态，显式市场特征反而引入噪声**
（80 维在 4/6 月领先，均值 +0.081 vs +0.067；88 维仅在 2026-06 极端月显著改善）。
这与树模型相反——T2(88维) 月均 IC=+0.094，远超 T4(80维) 的 +0.037，
因为**树模型只能看截面数据，必须将市场状态编码为特征**。
**设计决策：T4 保持 80 维，T2 使用 88 维。非对称设计是正确的**（永久决策，不回头）。

---

## 3. 数据体系

### 3.1 四轨制数据源（2026-07-17 起）

| 数据 | 主力源 | 后备源 | 变更日期 |
|------|--------|--------|----------|
| OHLCV 日线 | Tencent/Sina | baostock | 2026-07-17 |
| 实时 PE/PB | 腾讯行情 (qt.gtimg.cn) | - | 2026-07-24 |
| 估值(PE/PB/PS/PCF) | **TDX/mootdx** | baostock | **2026-07-24** |
| 指数日线 | baostock | Tencent | - |

**TDX/mootdx** 通过 `sequoia_x/data/tencent_source.py::TDXSource` 封装，
`sync.py::_fill_valuation_gaps` 中 Phase 3a(TDX) → Phase 3b(baostock) 双保险。
baostock 代码完整保留，TDX 不可用时自动切换。

**⚠️ volume 单位混用教训（未完全修复）**：Tencent fqkline 返回**手**（lots），baostock/Sina 返回**股**（shares）。
实测约 10% 股票（~500 只）在历史回填时混入"手"（ratio≈1x），其余为"股"（ratio≈100x）。
已修复源头（`_tencent_kline` 加 `volume * 100` 转换），**存量未修复**（特征 Z-score 归一化消除量级差异，对回测影响有限）。

### 3.2 数据规模（2026-08-02 现状）

| 指标 | 值 |
|------|-----|
| 日期范围 | 2020-01-02 ~ 2026-07-31（1594+ 交易日） |
| 股票池 | ~2978 只（自动过滤 ST/次新/股价<2元/板块） |
| 主数据库 | `data/sequoia_v2.db`（stock_daily / index_daily） |
| 数据集缓存 | 80维=`62cf234c5440`、88维=`13132147f8e8`（40 万样本，142 采样日） |

### 3.3 采样日机制（训练样本的时间骨架）

**采样日**（`labels.py _get_sample_dates`）：每月取 2 个交易日（第 5 个、第 15 个）作为采样点。
每个采样日 = 全市场 ~2900 只股票各一条训练样本（120 天窗口 × 88 维特征 + 标签）。

**标签"答案揭晓"机制（核心概念，2026-08-02 澄清）**：
- y2 标签 = 采样日 + 未来 20 个交易日的超额收益——**像考试答案，要等 20 天后才"公布"**
- 例如 2026-06-22 采样日的 y2 答案在 ~7/21 公布（7 月数据已同步 → 8 月重训可用）
- **2026-07 的采样日（7/7、7/21）在 8 月初答案未公布**（需 8 月价格）→ **不能用于 8 月重训**
  ——这不是缺陷，是**防未来数据的正确行为**（用了才是作弊）；9 月重训时自然可用

### 3.4 数据集缓存机制

- 缓存 key：`n_stocks + sample_start + sample_end + window + feature_version + market_state` 的 MD5
- **⚠️ key 不含采样日数量 → 缓存不会自动过期** → 数据更新后需手动 `force_rebuild=True` 重建
- 重建脚本：`scripts/rebuild_dataset_cache.py`（80+88 维并行，各 12 workers）
- 重建耗时：~2.5h（142+ 采样日 × 2900 只）

---

## 4. 预测缓存与训练架构

### 4.1 预测缓存（prediction_cache.json）

**结构**：`{month: {symbols, t2, t1, t3, t4}}`——每月一个条目，含全市场股票预测。
- 缓存键月份 = **执行月**（如 `2026-08` 表示 8 月买入用的预测，预测基于 7/31 数据）
- 回测与模拟盘共用此缓存（回测读缓存 → 模拟盘重训时增量更新）
- 2026-08-02 现状：72 个月（2020-09 ~ 2026-08）

### 4.2 build_prediction_cache.py（T2/T1/T3 构建）

- 架构：master-Pool 模式，"每月一个 worker"；单月任务主进程直接执行（daemon Pool 嵌套限制，见 §20.4）
- **特征构建并行化**（2026-08-02）：Step5 用 `ProcessPoolExecutor`（非 daemon）8 进程分块，
  单月特征提取从 30-40min → ~5min（6-8 倍加速）
- 断点续跑：启动时加载已有缓存，`pending = test_months - cache` 自动跳过
- 并发：多月任务 24 workers（n_jobs=1，OMP=1，KMP_AFFINITY 清除）

### 4.3 t4_monthly_worker.py（T4 训练）

- 独立 Worker：T4 LSTM 单月训练 + 特征提取 + 预测（80 维缓存）
- 参数：`ALL_MONTHS` 动态生成（2020-08 ~ 2026-12）
- 断点续跑：`.t4_tmp/t4_{month}.json` 完成标记
- 训练：200 epochs，best_epoch early stop（实测 8 月 14.6min）
- 特征提取+预测：~0.65s/只 × 2978 ≈ 30min（单线程，T4 worker 内未并行化）

### 4.4 线程与并发铁律（KMP_AFFINITY，详见 §20.1）

- `.bashrc` 有 `KMP_AFFINITY=granularity=fine,verbose,compact,1,0` + `OMP_NUM_THREADS=36`
- **任何多进程并行程序必须**：`os.environ.pop("KMP_AFFINITY", None)` + `OMP_NUM_THREADS=1`（import numpy 前）
- 线程预算：`worker 数 × OMP(1) ≤ 36`；训练用框架自身并行参数（LightGBM n_jobs），BLAS/OMP 一律 1
- 修复位置：`t4_monthly_worker.py`（pop + TF 2/1 + OMP=2）、`build_prediction_cache.py`（pop + 四件套）

---

# 第二部分：验证与优化历程

---

## 5. Walk-Forward 验证框架

### 5.1 什么是 Walk-Forward

Walk-Forward（滚动前向验证）是量化策略最严格的检验方式——**模拟真实交易场景**：用过去的数据训练，
在未来的数据上测试，然后不断向前推进。这样设计是为了彻底杜绝**前视偏差（look-ahead bias）**
（模型在训练时"看到"了未来的数据）。

```
Fold 3: 用 2020-2024 数据训练 → 在 2025 年测试（模拟站在 2025 年初的你）
Fold 4: 用 2020-2025Q1 训练 → 在 2025 Q2-Q4 测试
Fold 5: 用 2020-2025 训练 → 在 2026 H1 测试
Fold 6: 用 2020-2026Q1 训练 → 在 2026 Q2 测试
```

Walk-Forward 中的 `purge_gap = 22 个交易日` 是额外安全保障——训练集最后一天与测试集第一天之间
至少间隔 22 天，防止训练集标签信息「泄露」到测试集。

### 5.2 初始 Walk-Forward 结果（2026-07-29 前）

| Fold | 测试期 | T2 Rank IC | T4 Rank IC | 方向胜率 |
|------|--------|-----------|-----------|---------|
| 3 | 2025 全年 | +0.117 | +0.071 | 55.1% |
| 4 | 2025 Q2-Q4 | +0.141 | +0.101 | 53.1% |
| 5 | 2026 H1 | **-0.258** | **-0.258** | 0.0% |
| 6 | 2026 Q2 | +0.156 | -0.091 | 0.0% |

### 5.3 2026 年市场风格切换（核心背景）

| 时段 | y2 均值 | y2 标准差 | y2>0 比例 |
|------|---------|----------|----------|
| 2025 全年 | +1.2% | 0.106 | 48% |
| 2026 H1 | **-10.8%** | **0.224** | 32% |
| 2026 Q2 | **-21.7%** | **0.252** | **仅 20%** |

2026 年市场风格剧变（仅 20% 股票跑赢沪深 300）——半年度 Fold 5 的 IC 崩塌（-0.258）正是风格切换的体现。
**滚动窗口测试证明：缩短训练窗口 + 纳入近期数据可逐步改善 IC（-0.258→-0.156→-0.093）**——
这是月度重训练的出发点。

---

## 6. 小三步验证（2026-07-29，IC 层面）

**小三步目标**：验证三个独立改进在 IC 层面的效果（不涉及回测收益，只看选股排序质量）。

### 6.1 第一步：月度 Walk-Forward（12 月滚动 → 1 月测试）

| 月份 | y2 均值 | 80维 T4 IC | 88维 T4 IC | 方向 |
|------|---------|:--------:|:--------:|:--:|
| 2025-08 | -3.8% | +0.005 | — | 中性 |
| 2025-09 | -1.7% | **+0.090** | — | ✅ |
| 2025-10 | +3.4% | -0.002 | — | 中性 |
| 2025-11 | -0.1% | +0.045 | — | ✅ |
| 2025-12 | +2.4% | +0.053 | — | ✅ |
| 2026-01 | +2.1% | +0.066 | +0.049 | ✅ |
| 2026-02 | -0.5% | **+0.104** | +0.078 | ✅ |
| 2026-03 | -1.1% | **+0.210** | **+0.223** | ✅🔥 |
| 2026-04 | -1.8% | **-0.056** | -0.084 | ❌ |
| 2026-05 | **-42.0%** | +0.052 | +0.030 | ✅（崩盘月守正） |
| 2026-06 | -21.5% | **-0.153** | **-0.024** | ❌→⚠️（88维改善） |

**结论**：80 维 2026 月均 IC = +0.037（9/11 月 > 0），对比改进前 Fold5（2026H1）的 -0.258——
**月度 WF 将模型从不可用提升到可用**。这是月度重训练有效性的根本证据。

### 6.2 第二步：88 维 vs 80 维（市场状态特征）

| 月份 | 88 维 IC | 80 维 IC | 差值 | 结论 |
|------|:-----:|:-----:|:-----:|:----:|
| 2026-01 | +0.049 | +0.066 | -0.017 | 80 维优 |
| 2026-02 | +0.078 | +0.104 | -0.026 | 80 维优 |
| 2026-03 | **+0.223** | +0.210 | +0.013 | 持平 |
| 2026-04 | -0.084 | -0.056 | -0.028 | 80 维优 |
| 2026-05 | +0.030 | +0.052 | -0.022 | 80 维优 |
| 2026-06 | **-0.024** | -0.153 | **+0.129** 🔥 | 88 维优 |
| **均值** | **+0.045** | **+0.037** | — | — |

**结论**：80 维在 4/6 月领先 → **T4 保持 80 维**（LSTM 隐式推断市场状态）；T2 用 88 维（树模型需显式）。
非对称设计正确（见 §2.4）。

### 6.3 第三步：T2+T4 Rank 融合回测（11 个月）

| 周期 | TOP_N | 总收益 | 年化 | 夏普 | 最大回撤 | 月胜率 |
|------|:-----:|--------|------|------|----------|:----:|
| 2025年(5月) | 20 | **+25.8%** | **+73.5%** | **4.14** | 0.00% | 80% |
| 2026年(6月) | 10 | -40.3% | -64.4% | -0.41 | -65.2% | 67% |
| 全周期(11月) | 10 | -42.5% | -45.3% | -0.40 | -65.4% | 55% |

**小三步核心发现**：月度 WF + 融合在 2025 年表现出色（年化 +73.5%），但 2026 年 5-6 月两个月
合计亏损 -78~-89% 吃掉全部利润——**风控完全失效**（5 种风控模式收益完全相同，风控链路断裂）；
市场状态检测是滞后指标（月末反弹使其显示"正常"，错过月初暴跌）。

---

## 7. 大三步验证（2026-07-31，收益层面）

**大三步目标**：对 11 个月分别进行 **T4 LSTM 真实训练+预测**，与 T2 做 Rank 融合，跑完整 72 组回测。

| 步骤 | 内容 | 状态 |
|------|------|:----:|
| 大1 | T4 LSTM 11 个月 80 维真实训练 + 预测追加到缓存 | ✅ |
| 大2 | 72 组 T2(88维)+T4(80维) Rank 融合完整回测 | ✅ |
| 大3 | 最终分析报告 | ✅ |

### 7.1 大2 核心结果：T2+T4 vs 纯 T2

| 指标 | 小三步 纯T2 | 大三步 T2+T4 | 变化 |
|------|:-----:|:-----:|:--:|
| **全周期总收益** | **-42.5%** | **+55.9%** | **+98.4%** 🔥 |
| 全周期年化 | -45.3% | +66.7% | +112% |
| **全周期夏普** | **-0.40** | **+2.30** | **+2.70** |
| 全周期最大回撤 | -65.4% | **-14.9%** | -50.5% |

**关键发现**：**T4 是绝对主力**——加入 T4(80维) 后全周期从 -42.5% 翻到 +55.9%，回撤从 -65% 降至 -15%。
LSTM 捕捉到了树模型看不到的时序模式。

### 7.2 四项修复（2026-07-31 晚）

| 修复 | 文件:行 | 验证证据 |
|------|---------|---------|
| 市场状态前视偏差 | risk/manager.py:105 | 改用上月最后一天过滤 |
| T1 AUC阈值 | risk/manager.py:221 + monthly_engine.py:804 | T1过滤后: X只 |
| T4动态降权 | integration.py + monthly_engine.py:774 | T4信号弱(std=0.0042), T2权重=0.70 |
| T2分布空仓 | monthly_engine.py:780 | top100均值=-0.1489, 系统性看空→空仓 |

**修复后**：全周期 +89.5%、夏普 3.35、回撤 -12.5%（11 个月口径）。

---

## 8. 关键实验结论汇总（决策依据）

| 实验 | 结论 | 决策 |
|------|------|------|
| 扩窗口 vs 滚动窗口 | 扩窗口 3 月崩盘（-0.072 vs 滚动的 +0.223） | 滚动窗口 |
| 6 月 vs 12 月训练窗口 | 6月窗口 2026 均值 -0.026，12月 +0.094 | **12 月窗口** |
| 88 维 vs 80 维（T4） | 80 维 4/6 月领先 | T4=80 维 |
| 月度 vs 日频调仓 | 日频成本 > 收益 | 月度调仓 |
| TOP_N 对比（72 组） | TOP_N=10 综合评分最高 | TOP_N=10 |
| 风控模式（72 组） | M4（IC 加权）综合第一 | **M4** |
| Rank 融合 vs 数值平均 | 量纲不同（T2 ±0.3、T4 ±0.02） | **Rank 融合** |

### 8.5 T1 关闭（2026-08-02 实证）

- T1（XGBoost 5 日方向分类）70 个月真实 AUC=0.499 ≈ 随机（仅 2/70 月 >0.58 阈值）
- **方向可学性实验**（换目标全部 ≈0.5）：20 日超额方向（T2 回归方向化）AUC=0.5059、
  10 日方向 0.4993、20 日方向 0.4975——**A 股短期方向在 88 维日线特征下本质不可学**
  （T2 回归能提取微弱连续信号 IC≈0.03，离散化后信号消失）
- **处置**：T1 从风控体系移除（`monthly_engine.py` auc=0.5 保持关闭）；
  风控体系实际 = M2（T3 波动率仓位）+ M3（大盘状态）+ M4（IC 加权仓位）

---

## 9. T4 LSTM 艰难调试史（项目最深刻的一课）

### 9.1 问题表象

T4（LSTM-Transformer）在 Walk-Forward 验证中**预测值恒为常数**——对所有股票给出完全相同的预测。
val_loss = 0.0045 看起来正常但预测无效——**这是深度学习最危险的情况**。

### 9.2 四轮误诊

| 轮次 | 怀疑 | 结论 |
|------|------|:----:|
| 07-22 | Keras 序列化（load_model 破坏自定义层）→ 内存模型 | ❌ 仍退化 |
| 07-23 | Dropout 推理退化（降 dropout→0.1→0） | ❌ 仍退化 |
| 07-24 | 单核运行（KMP_AFFINITY 锁核，epoch 29min→64s 修复后） | ❌ 仍退化 |
| 07-28 | **找到真凶**（见下） | ✅ |

### 9.3 真凶（第四轮实验）

| 实验 | 配置 | pred_std | Rank IC |
|------|------|---------|---------|
| A | 纯LSTM + L2=0 | 0.045 | +0.055 ✅ |
| B | 纯LSTM + L2=1e-4 | 0.000 | 0 ❌ |
| C | LSTM+3T + L2=0 | 0.000 | 0 ❌ |
| D | 仅去Transformer | 0.037 | +0.089 ✅ |

**根因一：L2 正则化(1e-4)杀死 LSTM input kernel**——权重范数从 15.78 衰减到 0.00。
**根因二：Transformer 层稀释信号**——3 层 Self-Attention + LayerNorm 串联将 LSTM 提取的微弱时序模式逐步抹平。
**最终修复**：`lstm_l2_reg=0.0`, `num_transformers=0`, KMP_AFFINITY 清除, TF 线程显式设置。

### 9.4 元教训（为什么反复误诊）

1. 没有先验证权重本身就直接修改代码
2. 单次改了多个变量，无法确定因果关系
3. 为了迎合期望选择性解释数据

后续增加三条铁律：详尽日志+运行时自检、断点续跑、修复后必须验证（后扩展为六条，见 §21）。

---

# 第三部分：训练与回测

---

## 10. 数据扩展与缓存重建（60 个月扩展）

### 10.1 历史数据补齐（2026-07-31，三次迭代）

| 迭代 | 方法 | 结果 | 失败原因 |
|------|------|------|---------|
| v1 | `sync_daily(force=True)` + start_date=2020 | filled=0 | force 模式对已有数据的股票从 last_date 开始，不拉历史 |
| v2 | `backfill()` baostock 逐只 | 太慢（2.2s/只 × 5203 ≈ 3h）且 baostock 不稳 | 接口速度限制 |
| v3 | 直接调 `TencentSource.get_daily(code, days=1600)` | ✅ 成功 | — |

**最终结果**：日期范围 2020-01-02 ~ 2026-07-31（1594 交易日），总记录 736 万（317 万→736 万）。

### 10.2 缓存重建（2026-08-01）

数据扩展后必须重建缓存（采样日从 48 天 → 144 天）。代码改动：
- `features.py` / `labels.py` 新增 `include_market_state` 参数（T4=80维传 False，树模型=88维传 True）
- 缓存 key 增加 `market_state` 字段

新缓存（16 workers，总耗时 215.6 min）：80维=`62cf234c5440`、88维=`13132147f8e8`（40 万样本，142 采样日）。

### 10.3 7 月样本标签机制（2026-08-02 澄清，见 §3.3）

**结论**：7 月采样日（7/7、7/21）的 y2 标签需 8 月价格才可知 → 8 月重训时答案未揭晓 → 不能用于训练
（防未来数据的正确行为）；9 月重训时自然可用。**这不是缓存缺陷，是标签本质。**

---

## 11. 回测框架详解

### 11.1 月度回测循环（monthly_engine.py）

```
月末最后交易日：
  ① 训练：用过去 12 个月数据训练 T2/T3/T4（缓存模式跳过）
  ② 预测：全市场 ~2900 只打分（缓存模式直接读取）
  ③ Rank 融合 → 选 TOP N → 风控（T3 仓位/大盘状态/IC 加权）
  ④ 执行：次月第 1 个交易日开盘买入（佣金 0.025% + 滑点 0.01%）
  ⑤ 持有：每日检查 13 条卖出规则
  ⑥ 月末清仓（印花税 0.1%）→ 结算 → 下月
```

**T+1 模型（同 019 ETF 模拟盘）**：信号 T-1 日生成 → T 日开盘价执行；
每日期结顺序：先执行待卖出（T 日开盘）→ 再执行待买入（T 日开盘）→ 收盘价更新估值 + 卖出规则评估。

### 11.2 13 条卖出规则（总分 ≥ 60 触发）

| # | 规则 | 条件 | 分值 | 优先级 |
|---|------|------|------|-------|
| 1 | **硬止损 S1** | 亏损 ≥ 8% | **100** | 穿透 |
| 2 | 移动止盈 T1 | 盈利≥15% 且回落≥8% | 85 | 强 |
| 3 | 持有超时 D1 | >20 天 | 75 | 强 |
| 4 | 死叉确认 M | 连续3天 MA5<MA10 | 70 | 强 |
| 5 | 夏普恶化 SH | 15日夏普<-0.5 | 70 | 强 |
| 6 | 跑输大盘 R1 | 5日跑输>5% | 60 | 中 |
| 7 | 止盈预警 T2 | 回落≥5% | 50 | 中 |
| 8 | 夏普转负 SH | 10日夏普<0 | 50 | 中 |
| 9 | 止损预警 S2 | 亏损≥5% | 40 | 弱 |
| 10 | 即将超时 D2 | >15天 | 40 | 弱 |
| 11 | 死叉初现 M | MA5<MA10 | 40 | 弱 |
| 12 | 跑输预警 R2 | 5日跑输>3% | 30 | 弱 |
| 13 | 夏普偏低 SH | 15日夏普<0.5 | 30 | 弱 |

**保护规则**：T+1 保护（买入当日不卖出）、最低持有 <5 天不卖（硬止损除外）。
**V2 因子增强**：`prob_t1 < 0.3` +20 分、`excess_ret < -0.03` +15 分。

### 11.3 风控六模式（M0~M5）

| 模式 | 构成 | 说明 |
|------|------|------|
| M0 | 裸融合 | 基线 |
| M1 | +T1 方向过滤 | ❌ T1 已关闭（等价 M0） |
| M2 | +T3 波动率仓位 | 高波动股降仓（中性波动 25%，权重 clip(0.25/vol, 0.5, 1.5)） |
| M3 | +大盘状态 | 极端熊市 3 成仓、高波动 5 成、熊市 5 成、偏弱 7 成、结构分化 7 成、牛市 1.0 |
| M4 | +IC 加权仓位 | 排名靠前多买（第 1 名 > 第 10 名，线性递减） |
| M5 | 全风控 | M2+M3+M4 叠加 |

**T2 分布预警（空仓规则）**：最优 100 只 T2 预测均值 < -5% → 系统性看空 → 本月空仓。
实测触发月：2021-01（-5.65%）、2026-06（-14.89%）。

### 11.4 成本模型

| 项目 | 费率 | 买方 | 卖方 |
|------|------|:---:|:---:|
| 佣金 | 0.025% | ✅ | ✅ |
| 印花税 | 0.1% | ❌ | ✅ |
| 滑点 | 0.01% | ✅ | ✅ |

买入价 = open × 1.0001 × 1.00025；卖出价 = open × 0.9999；
最小单位 100 股（整手）；单只上限 = 总资金 × 0.9 / TOP_N。

---

## 12. 72 组全量回测（py312 最终口径）

### 12.1 执行

- 组定义：4 TOP_N × 3 时段 × 6 风控 = 72 组
- 并行：24 进程（`scripts/launch_backtest_parallel.sh`，~7 分钟）
- **环境：必须 py312**（铁律六——base 环境结果漂移 25%，见 §20.3）

### 12.2 最终最优配置（70 个月，2020-09~2026-06）

**M4 + TOP_N=10**：

| 指标 | 值 |
|------|-----|
| 总收益 | **+944.8%**（50 万 → 522 万） |
| 年化 | +52.1% |
| 夏普 | 2.28 |
| 最大回撤 | -18.2% |
| 月胜率 | 70% |
| 综合评分 | 0.836（72 组第一） |

### 12.3 三时段最优

| 时段 | 最优配置 | 收益 | 备注 |
|------|---------|-----:|------|
| 2025 年 | M5 + TOP_N=10 | **+25.1%** | 全年不低（旧结论 +3.4% 仅是 8-12 月窗口） |
| 2026 年 | M5 + TOP_N=10 | **+90.9%** | 年化 +307%，夏普 4.28 |
| 全周期 | M4 + TOP_N=10 | +944.8% | 综合评分 0.836 |

---

## 13. 八时段扩展回测（各年度明细）

| 时段 | 月数 | M0+10 收益 | **M4+10 收益** | M4+10 年化 | M4 夏普 | M4 回撤 | M4 月胜率 |
|------|:---:|:--------:|:----------:|:----------:|:------:|:------:|:--------:|
| 2020年 | 4 | +25.4% | +25.3% | +99.8% | 5.62 | -2.0% | 100% |
| 2021年 | 12 | +67.7% | +67.2% | +70.4% | 3.09 | -6.2% | 67% |
| 2022年 | 12 | +11.6% | +17.5% | +18.2% | 0.95 | -12.8% | 42% |
| 2023年 | 12 | +55.1% | +58.3% | +61.3% | 2.78 | -6.6% | 75% |
| 2024年 | 12 | +10.4% | +11.1% | +11.5% | 0.52 | -18.1% | 67% |
| 2025年 | 12 | +21.7% | +25.1% | +26.1% | 1.69 | -7.4% | 67% |
| 2026年 | 6 | +72.9% | +90.9% | +307.5% | 4.28 | -13.5% | 100% |
| **全周期** | 70 | +757.4% | **+944.8%** | +52.1% | 2.28 | **-18.2%** | 70% |

**周期规律**：7 个时段全部正收益（无年度亏损）；弱年 = 震荡/熊市（2022 夏普 0.95、2024 回撤 -18.1%）；
极端年最强（2020 暴跌年 +25%、2026 崩盘年 +90.9% 月胜率 100%）。

---

## 14. 逐月 IC 分析（60 个月全貌）

### 14.1 方法

prediction_cache 70 个月 × 全部股票（~2900 只）Spearman（预测 vs 实际 20 日超额收益，沪深 300 基准）。
脚本：`scripts/analyze_monthly_ic.py`；数据：`output/backtest_v2/monthly_ic_analysis.csv`。

### 14.2 按年度汇总

| 年份 | 月数 | T2 IC | T4 IC | 胜者 | y2 均值 |
|------|:---:|:-----:|:-----:|:----:|:------:|
| 2020 | 4 | +0.041 | +0.070 | T4 | -4.6% |
| 2021 | 12 | +0.002 | +0.015 | T4 | +2.9% |
| 2022 | 12 | +0.020 | +0.033 | T4 | +2.2% |
| 2023 | 12 | +0.026 | +0.018 | T2 | +0.7% |
| 2024 | 12 | -0.001 | +0.019 | T4 | -0.6% |
| 2025 | 12 | **-0.005** | **-0.006** | 平 | +1.5% |
| 2026 | 6 | +0.012 | -0.016 | T2 | -2.6% |

### 14.3 市场状态验证（假设被推翻）

| 市场状态 | 月数 | T2 IC | T4 IC | 结论 |
|----------|:---:|:-----:|:-----:|------|
| 温和涨 | 22 | **-0.026** | +0.000 | T4 优 |
| 温和跌 | 23 | +0.031 | +0.034 | 持平 |
| **熊市** | 13 | **+0.058** | +0.017 | **T2 显著强** |
| 牛市 | 12 | -0.014 | +0.012 | T4 优 |

**原假设"温和市 T2 强、极端市 T4 强"被 60 个月数据推翻**——实际：熊市（极端市）反而 T2 强、
牛市/温和涨 T4 强。**IC 口径说明**：IC 表的 idx_ret/y2 是"采样日 + 未来 20 日"跨月口径，不是自然月
（2026-06 的 -8.2% 实际覆盖 7 月大跌；6 月自然月 +2.79%、7 月自然月 -7.48%）。

### 14.4 核心发现

1. **2025 年两模型 IC 均 ≈ 0**（T2 -0.005 / T4 -0.006）——2025 收益低根因是模型排序能力本身弱
2. **T2/T4 优势年份分化**：2021-22/24 T4 强、2023/26 T2 强 → 动态权重有空间
3. **熊市 T2 IC 最高**（+0.058）→ 2026 高收益主要贡献者为 T2
4. 全部年份 IC 均值 0.00~0.05——单模型排序能力整体偏弱，融合与风控价值被低估

---

## 15. 7 月补测（2026-08-02）

### 15.1 背景

7 月不在原回测范围（到 6/30）。用户要求补验：6/30 预测在 7 月大跌中的表现。

### 15.2 过程（并行化实验载体）

1. build 2026-07（T2/T1/T3，ref_date=6/30）→ 特征并行化改造（见 §20.4）
2. T4 2026-07 训练
3. 回测 2026-07 单月（M4+TOP_N=10）

### 15.3 结果

| 指标 | 值 |
|------|-----|
| 沪深 300 | **-7.48%**（大跌，62% 个股下跌） |
| **策略收益** | **+3.63%**（超额 +11.1%） |
| 交易 | 20 笔（10 买 + 10 卖） |

**验证结论**：6/30 预测（无未来数据）在 7 月大跌中选股有效——未触发空仓（预测未看空），
选出的股票大跌中逆势 +3.63%。**与 6 月对比**：6 月空仓（错过微涨 +2.79%）、7 月买入（大跌中取胜）——
模型在下跌市的选择不是"躲"，而是"选出能跑赢的股票"（除非系统性看空才空仓）。

---

# 第四部分：模拟盘运行

---

## 16. 模拟盘架构

### 16.1 T+1 待执行订单模型（simulation 模块）

**递补规则**：
- 待执行信号按 LLM 评分降序，高分优先
- 买入信号严格 T+1：今日信号明日执行（`buy_date < today`）
- 每日期结：先执行待卖出（T 日开盘）→ 再执行待买入（T 日开盘）→ 收盘价估值 + 卖出规则评估
- LLM 推荐覆盖卖出（连续覆盖 ≤ 3 次，第 3 次强制卖出）
- 仓位不够时多余信号直接取消，不留存过夜

### 16.2 V2 与 LLM 完全隔离（2026-08-02 落地）

| 项 | LLM 模拟盘 | V2 模拟盘 |
|----|-----------|-----------|
| 数据库 | `data/sequoia_v2.db`（sim_* 表） | **`data/sim_v2.db`（独立 4 表）** |
| 资金 | 100 万 | **100 万（独立）** |
| 信号来源 | LLM 推荐（strategy_from="LLM推荐"） | **V2 模型（strategy_from="V2"）** |
| 持仓 | 共享 | **完全隔离** |

**实现**：`engine.py SimEngine` 增加 `db_path` 参数（V2 隔离关键，向后兼容）。

### 16.3 关键参数

| 参数 | 值 |
|------|-----|
| 初始资金 | 100 万 |
| 单只预算 | 总资金/20（均分，不设绝对上限） |
| 最多持仓 | 20 只 |
| 每月买入 | 10 只 |
| 最低持有 | 5 天（T+1 保护） |
| 最长持有 | 20 天（对应 y2 周期） |
| 调仓 | 每月 1 次 |

---

## 17. 月度重训管线（时间线设计）

### 17.1 事件驱动时间线（用户设计，2026-08-02）

```
日常交易日 18:10 起：
  数据同步（~34min）→ LLM 选股（3min）→ LLM 模拟盘操作（1min）
  → 【V2 模拟盘操作（1min）】→ 日报推送 ×2（LLM + V2）
月末最后交易日操作完成后：
  → 下月 1 日 00:00 cron 自动重训（上月数据完整，凌晨训练不占盘面）
重训完成（信号入库）：
  → 下个交易日晚上 LLM 操作后执行 V2 买入（当天开盘价，收盘价算当日收益）
  → 若 1 日非交易日自动顺延（T+1 模型天然支持）
```

### 17.2 8 月提前重训实践（2026-08-02）

7 月数据完整（7/30 最后交易日）→ 周日（8/2）提前重训 → 8/3 周一晚即可买入，8 月不空窗。
**全程自动链**：缓存重建（force_rebuild）→ 监督链（build 2026-07/2026-08 → T4 → 选股 → 入库 → 推送）。

### 17.3 月度重训脚本（scripts/v2_monthly_retrain.py）

```
Step1: T2/T1/T3 预测缓存构建（build_prediction_cache，增量断点续跑）
Step2: T4 LSTM 训练（t4_monthly_worker）
Step3: Rank 融合选股（T2 top100 预警 → 空仓风控）
Step4: 信号入库 sim_v2.db（submit_buy_signals，strategy_from="V2"）
Step5: 荐股推送（wxpusher）
Step6: 上月模拟盘月度报告推送
```

**选股风控**：T2 top100 均值 < -5% → 空仓（与回测一致，2026-06 实测触发）。

### 17.4 训练耗时（实测）

| 步骤 | 耗时 |
|------|:----:|
| T2 训练 | ~2min |
| T3 训练 | ~20min（CatBoost 高维特征单线程） |
| T4 训练 | ~15min（best_epoch early stop） |
| 特征提取+预测 | ~5min（并行化后）|
| **总计** | **~1-1.5h** |

**不耽误买卖**：信号 T+1 执行（明晚开盘），当晚训练完成即可；买卖执行由 pipeline V2 步骤独立处理。

---

## 18. 荐股/买卖/递补逻辑（与 LLM 一致）

### 18.1 选股逻辑（V2 信号生成）

```
A. 过滤：排除已持仓/涨停/ST
B. T2+T4 Rank 融合排名
C. T3 波动率调整（年化波动 >60% 降权 20%）
D. 取 TOP 10 → 写入 sim_v2.db（strategy_from="V2"）
```

### 18.2 买卖执行（与 LLM 模拟盘同一引擎）

- 买入：T 日开盘价（检查涨停/停牌），信号来自 T-1 日
- 卖出：T 日开盘价（检查跌停/停牌），决策来自 T-1 日 CLOSE 评估
- 递补：卖出释放仓位 → 待买入信号递补
- 移动止盈：盈利 ≥15% 激活，从高点回落 ≥8% 强卖（85 分）、≥5% 预警（50 分）
- 交易成本：佣金万 2.5 双向、印花税千 1 卖出、滑点万 1
- 高价股自动取消：单只预算 5 万不够 1 手时取消（如贵州茅台）

---

## 19. 报告体系

| 报告 | 频率 | 内容 |
|------|------|------|
| 单股平仓报告 | 每笔卖出 | 卖出明细 + 盈亏 |
| 组合日报 | 每交易日 | 持仓/买卖/估值/明日待卖出（LLM + V2 双份） |
| 月度汇总报告 | 每月（重训后） | 胜率/盈亏比/夏普/回撤（V2 独立） |
| 荐股报告 | 每月重训后 | V2 选股名单 + 预测摘要 |

**推送通道**：wxpusher（`settings.wxpusher_token` + `topic_ids`）。

---

# 第五部分：经验教训与铁律

---

## 20. 重大失误记录（血泪教训）

### 20.1 KMP_AFFINITY 锁核（两次，最严重）

**现象**：多进程 worker 全部卡死——进程活着但 CPU 利用率 1 核、训练寸步难行。
**根因**：`.bashrc` 的 `KMP_AFFINITY=granularity=fine,verbose,compact,1,0` 把所有进程主线程
**绑定到同一核心集**。8 进程 × 4 线程恰好错开绑定（正常）；16/24 进程时全部抢同一核心（卡死）。
**诊断标志**（5 分钟内定位）：
- 日志尾部出现 `OMP: Info #255: KMP_AFFINITY: ... bound to OS proc set`
- 总 CPU <15% 但进程数多；wchan=0（用户态自旋）；nonvoluntary 切换数千次
- **单 worker 测试正常（99%+）是迷惑点**（被绑到单核但无竞争者）

**影响**：T4 卡死 1 轮（浪费整晚）+ build 卡死 4 小时排查（2026-08-01）。
**修复**（双保险）：脚本内 `os.environ.pop("KMP_AFFINITY", None)` + 启动命令 `env -u KMP_AFFINITY`。
修复后 CPU 15%→94%，worker 各 96-99%。详见 BACKTEST_PLAN §26。

### 20.2 T4/build 并行时序错位

**现象**：T4 worker 训练完成但报 `月份不在 prediction_cache.json 中`——结果被丢弃（连环 17 个月份）。
**根因**：T4 快（70min/月）< build 慢（130min/月）→ T4 训练完时该月 T2/T1/T3 未构建 → `update_cache` 抛错。
**修复**：worker 不在缓存时存 `(symbols, t4)` 快照到 `.t4_tmp`（不抛错）；
`launch_t4_parallel.sh merge_results` 改为只更新 t4 字段（保留 t2/t1/t3）。
**教训**：T4 与 build 并行时，T4 worker 不得依赖 build 输出；merge 必须等 build 全部完成后跑。

### 20.3 环境一致性（2026-08-02，最隐蔽）

**现象**：同一缓存同一代码，图与数据不一致（plot 终值 +991% vs metrics +1003.76%）。
**根因**：base python3（numpy 1.26.4/scipy 1.13.1）与 py312（numpy 2.5.1/scipy 1.18.0）
跑回测结果**漂移 25%**（M4+10 全周期 +757% vs +945%）——numpy/scipy 版本差异 →
rankdata ties 处理/浮点顺序 → 排名细微差异 → 选股不同 → 收益累积放大。
**影响**：72 组/8 时段/文档数据全部重跑（py312）。**IC 等排序统计不受影响**（重跑验证一致）。
**铁律**：所有运行统一 py312（铁律六）。详见记忆 environment-consistency-rule。

### 20.4 multiprocessing 嵌套（daemon 限制）

**现象**：build 的 Step5 特征并行（Pool 嵌套）静默失败——worker 无输出、无 Step6、缓存缺失。
**根因**：`multiprocessing.Pool` 的 worker 是 **daemon 进程**，禁止再创建子进程
（`AssertionError: daemonic processes are not allowed to have children`）。
**修复**：单月任务改为主进程直接执行（非 daemon）+ `ProcessPoolExecutor`（非 daemon）特征并行。
**验证**：CPU 3%→22.8%（8 核），特征构建 30-40min→5min。

### 20.5 数据口径错误（IC 表 vs 自然月）

**现象**：把 IC 分析表的"2026-06 大盘 -8.2%"误读为 6 月自然月——实际是"6/22 采样日 + 未来 20 日"
（跨 7 月大跌）；**6 月自然月 +2.79%（涨）**、7 月 -7.48%（跌）。
**影响**：一度错误解释"6 月空仓躲过大跌"（实际错过微涨）。已修正 BACKTEST_GUIDE §3.3 口径标注。
**教训**：数据结论先查证（数据库自然月），口径必须标注。

### 20.6 其他教训（简要）

- **缓存陈旧**：数据集缓存 key 不含采样日数 → 数据更新后不自动过期 → 需手动 force_rebuild
- **状态栏转义**：python -c 字符串替换把 `'\n'` 写坏（SyntaxError 两犯）——改 .py 用 Edit 工具 + py_compile
- **kill 与启动同命令**：pgrep 匹配自身命令行误杀（多次 exit 144）——kill 与启动分开
- **volume 单位混用**：Tencent 手 vs baostock 股（~500 只存量未修复）
- **上下文浪费**：1M 上下文需控制输出（大输出限行、大文件分页）

---

## 21. 铁律体系（六条，2026-08-02 最终版）

| # | 铁律 | 要点 |
|---|------|------|
| 一 | **长时间任务必须多进程并行** | >1h 任务可分解必须并行；线程预算：总线程 ≤ 核数 × 1.5；KMP_AFFINITY 必须清除 |
| 二 | **长时间任务必须 nohup 解绑** | PPID=1；独立日志；PID 文件；完成标记；启动命令 `env -u KMP_AFFINITY` |
| 三 | **修复代码后必须验证生效** | 运行时检查（grep 日志确认/小批量验证）；禁止改完就声称修好 |
| 四 | **断点续跑，同命令恢复** | 增量保存（原子单元完成即写盘）；启动检测跳过已完成；同命令续跑 |
| 五 | **详尽日志 + 运行时自检** | 跑到哪/还要多久/中间结果三问；启动诊断（CPU/线程/参数）；输出验证（std>0 等） |
| 六 | **统一生产环境 Python** | 一切运行用 py312（`/home/zhulei/anaconda3/envs/zhulei_py312/bin/python`）；禁止裸 python3（环境差异漂移 25%） |

**元教训**（T4 调试四轮误诊）：先验证权重再改代码、单次只改一个变量、不迎合期望解释数据。

---

## 22. 优化方向（2025 收益优化 + 待办）

### 22.1 §25 优化方案（2026-08-01 记录，2026-08-02 验证）

| 方案 | 结论 |
|------|------|
| 方案 1：滚动 IC 加权融合 | ❌ **证伪**（70 个月全量劣于 pred_std：+693% vs +757%，回撤更大）——滚动 IC 预测性不足（优势年份切换频繁），IC<0 取 0 在 IC≈0 月份极端化 |
| 方案 2：分散约束 | ⏳ 待做（单只/行业上限） |
| 方案 3：TOP_N 自适应 | ⏳ 待做（温和市分散/极端市集中） |
| 方案 4：T2 强化 | ⏳ 待做（Optuna 50→100 trials、特征扩展） |
| 方案 5：成本优化 | ⏳ 待做（月均换手 950%，交易成本侵蚀） |

**2025 年收益认知修正**：全年 +25.1%（py312）不低——之前 +3.4% 只是 8-12 月窗口；
2025 年两模型 IC≈0（排序能力弱）是真实边界。

### 22.2 T1 关闭（见 §8.5）

方向预测任务在日线特征下本质不可学——换目标/换模型均无效（AUC≈0.5）。

### 22.3 待办清单（2026-08-02 状态）

- [x] 60/70/71 个月回测（py312 最终口径）
- [x] 8 月提前重训（荐股入库，8/3 买入）
- [x] 模拟盘代码落地（日常/重训/隔离/报告）
- [ ] 模拟盘实盘验证（8/3 起每日跟踪）
- [ ] 特征提取并行化在 T4 worker 的应用（当前仅 build）
- [ ] 方案 2/3/4/5（分散约束/T2 强化等）
- [ ] volume 存量修复（API 空闲时 ~500 只）
- [ ] 9 月 1 日 cron 重训自动验证

---

# 第六部分：操作手册

---

## 23. 常用命令速查

```bash
# ═══ 统一生产环境（铁律六）═══
PY=/home/zhulei/anaconda3/envs/zhulei_py312/bin/python

# ═══ 数据同步（cron 18:10 自动）═══
$PY pipeline/pipeline.py --sync-only        # 或 main.py --sync-only

# ═══ 预测缓存构建（T2/T1/T3）═══
$PY scripts/build_prediction_cache.py --start-month 2026-08 --end-month 2026-08 --skip-t4

# ═══ T4 训练（单月）═══
$PY scripts/t4_monthly_worker.py --month 2026-08

# ═══ 数据集缓存重建（数据更新后）═══
$PY scripts/rebuild_dataset_cache.py        # 80+88 维并行

# ═══ 回测 ═══
$PY scripts/run_comprehensive_backtest.py --period 全周期 --top-n 10 --mode M4 \
    --start-month 2020-09 --end-month 2026-07 --fusion-method pred_std
bash scripts/launch_backtest_parallel.sh pred_std 24   # 72 组并行

# ═══ 分析 ═══
$PY scripts/analyze_monthly_ic.py           # 逐月 IC
$PY scripts/analyze_t1_auc.py               # T1 AUC
$PY scripts/plot_full_period_curve.py       # 全周期曲线（含 7 月）
$PY scripts/plot_backtest_results.py --with-curve --csv output/backtest_v2/summary_all_60m.csv

# ═══ 模拟盘 ═══
$PY scripts/v2_simulation_daily.py          # V2 日常操作（pipeline 自动）
$PY scripts/v2_monthly_retrain.py           # 月度重训（cron 每月 1 日 00:00）

# ═══ 后台状态 ═══
ps -eo pid,etime,pcpu,args | grep -E "build_prediction_cache|t4_monthly"
bash scripts/launch_t4_parallel.sh --status
```

## 24. 后台任务管理

- **T4 并行**：`launch_t4_parallel.sh`（start/status/merge/kill），12 并发，`--merge` 合并 t4
- **回测并行**：`launch_backtest_parallel.sh`（24 进程，环境变量 TOP_N_OVERRIDE/MODES_OVERRIDE/PERIODS_OVERRIDE）
- **重训自动链**：cron `0 0 1 * *` → v2_monthly_retrain.py；pipeline 步骤 v2_simulation（LLM 后）
- **监控**：`logs/` 按日期命名；铁律五三问日志（进度/ETA/指标）

## 25. 文件索引

### 25.1 核心代码（sequoia_x/model_selection_v2/）

```
config.py              # V2 全局配置（窗口/参数/线程/Optuna）
features.py            # 88/80 维特征工程（ref_date 过滤防前视）
labels.py              # 标签构建 + 采样日 + 数据集缓存
train.py / evaluate.py # Walk-Forward 训练/评估（断点续跑）
integration.py         # Rank 融合（rank_fusion / rank_weighted_fusion）
models/
  tree_reg.py          # T2 LightGBM 回归
  tree_cls.py          # T1 XGBoost 分类（已停用）
  tree_vol.py          # T3 CatBoost 回归
  deep_lstm.py         # T4 LSTM（纯 LSTM，KMP 修复）
backtest/
  monthly_engine.py    # ★ 月度回测引擎（周期/信号/持仓/结算/IC采集）
  engine.py            # 回测辅助（买卖执行）
  config.py            # 成本/滑点/卖出规则
risk/
  manager.py           # 风控（市场状态/T2分布预警）
  sizer.py             # 仓位调节（T3 波动率 / IC 加权）
simulation/            # 模拟盘（T+1 模型、13 条规则、递补、报告）
```

### 25.2 运行脚本（scripts/）

```
build_prediction_cache.py   # 预测缓存构建（24 进程/主进程+特征并行，断点续跑）
t4_monthly_worker.py        # T4 单月训练 Worker
launch_t4_parallel.sh       # T4 启动器（start/status/merge/kill）
launch_backtest_parallel.sh # 72 组回测并行启动器
run_comprehensive_backtest.py # 回测主程序（--fusion-method 可切换）
v2_simulation_daily.py      # V2 模拟盘日常（买卖+日报+月末检测）
v2_monthly_retrain.py       # V2 月度重训（训练+选股+入库+荐股+月报）
analyze_monthly_ic.py       # 逐月 IC 分析
analyze_t1_auc.py           # T1 AUC 分析
plot_full_period_curve.py   # 全周期曲线（双面板，只标最大回撤）
plot_backtest_results.py    # 全量图表（净值/热力图/雷达等）
rebuild_dataset_cache.py    # 数据集缓存重建（80+88 维）
```

### 25.3 数据与输出

```
data/sequoia_v2.db              # 主数据库（stock_daily/index_daily）
data/sim_v2.db                  # V2 模拟盘独立库（4 表）
data/archive/sim_lstm_*.db      # V1 模拟盘归档（已停用）
data/cache/v2_dataset/62cf234c5440/   # 80 维数据集缓存（T4）
data/cache/v2_dataset/13132147f8e8/   # 88 维数据集缓存（T2/T1/T3）
output/backtest_v2/prediction_cache.json  # 72 个月预测缓存
output/backtest_v2/summary_all_60m.csv    # 72 组汇总（py312）
output/backtest_v2/summary_all_periods.csv# 8 时段汇总（py312）
output/backtest_v2/charts/*.png           # 全部图表（py312 重制）
logs/                                   # 全部运行日志
```

---

## 结语

V2 框架历经 V1 演进、T4 调试、小/大三步验证、60 个月数据扩展、环境一致性修正、
模拟盘落地，最终形成**"T2(88维)+T4(80维) Rank 融合 + 月度重训 + M4 风控 + 月度调仓"**的完整体系。
70 个月全量验证（py312）：全周期 +944.8%、年化 52%、夏普 2.28、回撤 -18.2%、月胜率 70%。
**优势**：无年度亏损、极端市防守+选股双强（2026 崩盘年 +90.9%、7 月大跌月 +3.63%）。
**边界**：震荡/熊市年收益低（2022/2024）、单模型 IC 整体偏弱（0.00-0.05）。
**后续**：模拟盘实盘验证、T2 强化、分散约束、成本优化（见 §22）。
