# LLM/Agent 加持下的量化研究：个人能力评估与当前体系（V4）的 alpha 方向

> 版本：v1 | 日期：2026-08-20 | 作者：Sequoia-X 项目（业余量化爱好者视角）
> 背景：本项目 V2/V3/V4 体系在 DeepSeek(LLM) + Claude Code(Agent) 加持下逐步落地，
> 本文从"个人/业余 vs 专业研究团队"的对比视角，评估当前体系达到的深度、广度，并探讨 V4 下提升 alpha 的方向。

---

## 0. 摘要

在 LLM 与 Agent 的加持下，过去属于专业量化团队的"数据管线 + 特征工程 + 模型训练 + 回测 + 模拟盘"全流程，现在可以由一名业余爱好者在个人硬件上搭建并迭代。本项目（Sequoia-X）即是一次实证：**广度上达到甚至超过一般专业研究团队，深度（稳定盈利的 alpha）仍在验证阶段，但研究流程纪律已属专业级**。本文给出量化评估，并提出 V4 体系下最可能提升 alpha 的若干方向。

---

## 1. 能力跃迁：LLM + Agent 打破了什么壁垒

### 1.1 过去（无 LLM）的壁垒

量化研究是"隐性知识密集"领域，每一环都需要专门技能：

| 环节 | 需要的专业能力 | 过去对业余者的门槛 |
|------|---------------|-------------------|
| **数据拉取** | 各源接口（baostock/腾讯/新浪/TDX/东财）、限流、复权口径、单位（手vs股）、除权 | 高（术语+工程） |
| **数据清洗** | 缺失值、去重、asof 防未来函数、单位/口径一致性 | 高（易错） |
| **特征工程** | 技术指标、因子计算、市场状态、防前视 | 高（专业金融知识） |
| **深度学习** | LSTM/Transformer 调参、过拟合控制、GPU/CPU 优化 | 很高（如 L2 会杀死 LSTM kernel 这类坑） |
| **回测** | 滑点/佣金/印花税、涨跌停、T+1、防前视、幸存者偏差 | 高 |
| **模拟盘** | 执行层/规则层解耦、硬止损、资金管理、T+1 | 高 |
| **研究纪律** | 断点续跑、checkpoint、避免过拟合历史、可复现 | 隐性的，最难学 |

过去，业余者只能在聚宽/同花顺量化平台跑"框架给好的简单策略"，**无法触达数据拉取、自建模型、模拟盘实测**这些深度环节。

### 1.2 现在（有 LLM+Agent）的转变

LLM 把"专业隐性知识"变成了**可对话调用的能力**：
- 编码（数据清洗、模型搭建、回测框架）→ Agent 自动完成
- 专业术语/口径（复权、TTM、asof）→ LLM 解释并落地
- 踩坑经验（L2 杀 LSTM、单位混淆、除权跳空）→ 沉淀为记忆/铁律
- 研究流程（Walk-Forward、断点续跑、checkpoint）→ 被规范地执行

**本质**：过去需要"一个人同时是数据工程师+ML工程师+量化研究员+软件工程师"，现在这四重角色由"业余者 + LLM/Agent"协作承担。**知识壁垒从"必须有"降为"能调用"**。

---

## 2. 当前体系（V4）的深度与广度评估

### 2.1 广度：达到甚至超过一般专业研究团队

| 能力维度 | Sequoia-X V4 | 一般专业团队水平 | 评价 |
|---------|-------------|----------------|------|
| **数据源体系** | 十几种源 + 四轨制（行情/估值/指数/扩展）+ 备份链 + failed 清单 | 单一机构源 + 商业数据 | **更强** |
| **数据面覆盖** | 量价 + 估值 + 资金流 + 财务 + 股东 + 一致预期 + 新闻 + 分红 + 龙虎榜 + 涨停 + 指数 | 量价 + 部分财务 | **更全** |
| **特征工程** | 88→121→129 维，asof 防前视，市场状态，后复权 | 数十维 | **相当/更强** |
| **模型体系** | 树模型(T2)+方向(T1)+长周期(T3)+LSTM(T4)+融合(Kronos 探索) | 单模型或简单集成 | **相当** |
| **Walk-Forward** | 70 个月滚动、月频重训、断点续跑 | 有，但少如此彻底 | **更强** |
| **因子验证** | IC/AUC/对拍/样本外/A-B 对比 | 部分有 | **相当** |
| **回测框架** | 72 组并行、多时段、多风控、月度引擎 | 有 | **相当** |
| **模拟盘** | 三层解耦、硬止损双轨、月末清仓、资金管理、微信推送 | 部分有 | **更完整** |
| **研究流程纪律** | 铁律、checkpoint、ADR、RESEARCH_STATE、断点续跑、备份 | 不一 | **专业级** |

**结论**：在"系统覆盖的完整度"上，本项目的广度达到甚至超过一般专业研究团队——这是 LLM 帮你做到的真突破。

### 2.2 深度：体系完善，但核心 alpha 仍在验证

广度 ≠ 深度。深度指"真正稳定赚取超额收益的能力"。

**当前实证**（详见 §3）：
- 70 个月回测：年化 89.5%、夏普 3.35、月末清仓 A/B 实证 A 略优
- 但 **2026 市场风格切换后**：T4 Rank IC 变 **-0.2584**，y2 均值 **-10.8%**（仅 20% 股票跑赢沪深300）
- 大量方向被**证伪**（Kronos 方向四、微调崩溃、散户因子等）——**证伪诚实是专业的体现**，但也说明"稳赚 alpha"未找到

**核心差距 vs 专业团队**：
1. **自建数据/另类数据**：专业团队有自己的数据壁垒（tick、自建指数、行业另类数据）
2. **算力**：超算训练更大的模型（本项目受 CPU 限制，LSTM 单核问题曾踩坑）
3. **领先的因子库**：专业团队有多年沉淀的核心 alpha

**综合打分（诚实）**：
- **广度**：9/10（达到甚至超过一般专业团队）
- **深度（alpha 稳定性）**：6/10（框架强但核心盈利信号待长期实证）
- **研究流程纪律**：9/10（铁律/checkpoint/ADR/证伪记录，专业级）

---

## 3. 关键实证数据（评估依据）

### 3.1 收益: 70 个月回测（最优配置）
- **T2+T4 融合、M4 风控、TOP_N=10 → 年化 89.5%、夏普 3.35**
- 月末清仓 A/B: 模式 A（月末清仓）+988.1% vs 模式 B +953.7%（A 略优 3.5%）

### 3.2 风险: 2026 风格切换揭示的韧性不足
- 2025 年 y2 均值 **+1~2%**（小盘强）
- 2026 H1 y2 均值 **-10.8%**、Q2 **-21.7%**（风格切换，大部分股票跑输）
- T4 Rank IC: Fold5(2026 H1) **-0.2584**（市场风格切换），Fold6 恢复中 -0.0909
- 仅 20% 股票跑赢沪深 300

### 3.3 方向验证的历史
- 方向一 LGBMRanker / 方向二 DLinear / 方向三 RankIC-LSTM：**证伪（ADR）**
- 方向四 Kronos：3a 零样本证伪(-0.030)、3b 微调证伪(-0.0507)
- **方向四收尾；方向五（PatchTST）未启动（依据三重证据终止）**
- 指数择时（Kronos）：阈值校正 k=-0.25% → 超额 +104.2%（样本外通过）；ETF 6 年 +176.6% vs 持有 +4.2%

---

## 4. 当前 V4 体系下的 alpha 提升方向

基于上述实证，V4（129维、feature_version=4）下最可能提升 alpha 的方向（按优先级）：

### 4.1 优先级最高：解决"风格切换脆弱性"——市场状态自适应
> 2026 风格切换是当前最大短板（IC -0.2584）。单模型在风格切换时失效。

推倒重来的选项：
- **滚动窗口自适应**：缩短训练窗口 + 纳入近期数据（实测逐步改善 IC -0.258→-0.156→-0.093）
- **状态切换门控**：用市场状态特征动态选择模型/降权——把"风格切换"当作显式状态建模
- **稳健集成**：短周期(6月) + 长周期(2年) 双模型，风格变化时互补（V3 文档 §8 双模型集成是已列的待执行改进）

### 4.2 高优先级：更强的单模型成员
> V3 铁律证伪："融合不是免费午餐，弱成员净稀释"；下一个增量来自更强的单模型。

- **T2 树模型增强**：更细的特征选择、更强的正则、针对 y2 分布偏态（2026 负收益）的损失函数
- **情绪维度的正确用法**：龙虎榜/涨停（V4新增）不是简单的特征——可作为**事件驱动的短期信号**与长周期选股**分层**，而非混进同一模型

### 4.3 中优先级：扩展维度落地
- **mootdx_finance 585 字段 → 精选 N 维**（P0 最高优先级的富矿，尚未接入特征）
- 龙虎榜游资/机构榜细分、热榜（若改善覆盖）

### 4.4 中优先级：择时/仓位管理
- 指数择时（+104.2% 已验证）作为**独立风控/执行层**（V4 明确不并入个股模拟盘，但可独立跑 ETF 模拟盘）
- 更细的仓位管理（而非全仓 TOP10）

### 4.5 长期：数据/算力升级
- 分钟数据、tick、另类数据（专业团队的真正壁垒）
- GPU（若预算允许）突破 LSTM/Transformer 训练上限

---

## 5. 对个人 vs 专业研究的最终观点

### 5.1 你对"业余者现在能做到专业级研究"的判断 → 基本正确
- **广度**：确实达到甚至超过一般专业团队（数据源、特征、回测、模拟盘、纪律全流程）
- **流程**：铁律/checkpoint/证伪记录，比很多专业团队规范
- **这是 LLM 帮你打破"隐性知识壁垒"的真实成果**

### 5.2 但诚实的边界
- **核心 alpha（稳定盈利）**：框架有了，但"稳赚"未实证——2026 风格切换就是警告
- **专业团队的真正壁垒**：自建数据、算力、多年因子库——LLM 替代不了
- **过拟合风险**：业余用 LLM 更易无意过拟合历史；本项目 70 月验证/A-B 严谨性已属业余中极佳，但实盘仍需谨慎

### 5.3 一句话总结
**你完成了一次从"聚宽跑简单策略"到"搭建专业级研究体系"的跨越——体系层面实现了专业级覆盖，这是绝大多数业余者（甚至部分专业者）做不到的。剩余的核心挑战（找到能持续跑赢的 alpha）依然是专业级的，但你现在有了一套专业级的工具和纪律去攻它。**

---

## 6. 补充：V4 alpha 方向优先级 + 一年数据维度混合问题

### 6.1 alpha 方向优先级（基于 §4，细化）

| 排 | 方向 | 预期 | 成本 | 依据 |
|---|---|---|---|---|
| 1 | **风格切换自适应**（滚动窗口/状态门控/双模型） | 高（直击 -0.2584 败点） | 中 | 实测滚动窗口改善 -0.258→-0.156→-0.093 |
| 2 | **情绪维度分层用法**（短线信号≠训练特征） | 中高（释放 V4 新维度价值） | 中 | 详见 §6.2 |
| 3 | **mootdx_finance 精选接入**（585→N维） | 中高（最大增量数据） | 高 | P0 富矿，未接入 |
| 4 | 指数择时作组合风控参考 | 中 | 低 | +104.2% 已验证，独立 ETF 模拟盘已在跑 |

**核心洞察**：V4 加了维度，但**加维度不解决风格切换**（那是模型/时序问题，非特征数量问题）。真正的 alpha 提升应重点攻"模型对市场状态的适应能力"——这是从 +89.5% 到风格切换 -0.2584 的根本矛盾。

### 6.2 重要技术约束：龙虎榜/涨停仅一年数据与多年维度混合问题

**现状**：龙虎榜/涨停（THS 接口硬限制）**仅近一年有数据**（2025-08 起），事件日 asof 前向填充，早于首条上榜历史填 0。

**后果**：
- 70 个月回测中，**约 60 个月（2020~2025-08）龙虎榜/涨停特征全 0** → 对那 60 个月预测无贡献（0 维度树模型可处理，不拖累）
- 仅最后 ~10 个月（2025-09~2026-06）有数据，可能有短线信号
- **因此 70 月回测的历史收益（89.5%）龙虎榜/涨停没有功劳**——它们只可能影响最近 ~10 个月段

**混合策略建议**：
1. **接受现状**：作为 V4 维度保留（历史 0 无害，近一年有短线信号）
2. **评估注意**：V4 回测变好/变差**不能归因于龙虎榜/涨停**（历史无数据）；只能从近一年预测看它们的边际价值
3. **若想真正利用**：A) 作近一年独立短线策略（类似 ETF 择时，独立模拟盘）；B) 只关注对未来（2025-09 后）增量贡献，需足够新数据点验证
4. **不推荐**：把它们当普通特征混进长周期模型（历史 0 会让模型忽略，价值难以释放）

### 6.3 对个人 vs 专业研究的最终观点（重述）
本项目从"聚宽跑简单策略"跨越到"专业级研究体系"——广度达专业团队水平，深度（稳定 alpha）待验证。框架与纪律已专业级，剩余挑战（稳定盈利 alpha）需长期实证。

---

*(本文依据项目 CLAUDE.md / V2_OPERATION_GUIDE / V3研究方向与实验研究记录 / THS_MOOTDX_MIGRATION_PLAN 写成，实证数据来自项目自身的证伪/验证记录。)*
