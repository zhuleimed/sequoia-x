# 因子体系构建指南（v1.0）

> **创建**：2026-08-08 | **定位**：为后续"自建因子库"专门项目提供完整蓝图
> **数据基础**：004_sequoia-x 已有资产（sequoia_v2.db / extra_features 121 维 / 评估基建）
> **读者**：本人（量化爱好者/初学者视角）——本文档自包含，不依赖对话上下文
> **状态**：规划文档（暂不实施，精力有限，后续视情况启动）

---

## 1. 什么是因子体系（入门）

**因子（Factor）**：一个可计算的"股票打分规则"。例如"过去 20 日涨幅"是动量因子，"市盈率倒数"是估值因子。每个因子给每只股票一个数值，用于排序选股。

**因子体系**：把大量因子（几百个）组织成有结构的库，统一计算、统一评估、统一筛选，最终合成一个选股信号。区别于本项目当前的"88 维零散特征"（为特定模型喂料），因子体系是"以因子为中心"的管理方式——每个因子是独立可检验的资产。

**三个核心指标（评估因子的标尺）**：

| 指标 | 定义 | 公式 |
|------|------|------|
| **IC**（信息系数）| 因子值与未来收益的 Spearman 相关系数（截面）| `spearman(factor, future_ret)`，按月截面计算，逐月得到一个 IC 序列 |
| **ICIR** | IC 均值 / IC 标准差——因子稳定性的度量 | `ICIR = mean(IC序列) / std(IC序列)` |
| **分层收益** | 按因子值分 10 组（D1~D10），看各组未来收益的单调性 | D10（最高组）应显著高于 D1（最低组）|

**为什么需要体系化**：Kronos 3a 的教训——单期高 IC 可能是市场效应（均值回归在下跌月强）；只有**长周期、带基线对照、多指标交叉**验证的因子才是可信资产。

---

## 2. 已有数据资产盘点（可直接复用）

### 2.1 行情数据（sequoia_v2.db，主力库）
- `stock_daily`：全 A 日线 OHLCV（2020-01 起 ~3000+ 只 × ~1600 交易日）
- `index_daily`：指数日线（沪深300 sh.000300 等，评估基准）
- 注意事项：**amount 字段腾讯源基本未入库**（NaN）——计算因子需 `volume×均价` 估算或忽略；复权/涨跌停/停牌处理见 §8

### 2.2 扩展维度（data/extra_features/，121 维特征工程已就绪）
| 数据面 | 落盘位置 | 可用因子方向 |
|--------|---------|-------------|
| fund_flow 主力资金流 | extra_features/fund_flow/*.parquet | 主力净占比、5 日均值、超大单占比、净流入天数 |
| finance 财务摘要 | extra_features/finance/*.parquet | ROE/ROA/毛利率/营收同比/负债率（披露日对齐）|
| holders 股东结构 | extra_features/holders/*.parquet | 股东人数环比（筹码集中度）、户均市值 |
| consensus 一致预期 | extra_features/consensus/*.parquet | 评级分布、预测 EPS→预测 PE、预测修正 |
| news / news_cls | extra_features/news/*.parquet + 财联社快讯 | 近 N 日新闻条数（数量信号）、词表情绪 |
| xdxr 分红回购 | extra_features/xdxr/*.parquet | 股息率、近 3 年分红次数 |
| forecast 业绩预告 | extra_features/forecast/*.parquet | 预告类型、净利润增幅上下限（发布日 asof）|

### 2.3 评估基建（本项目已验证的口径，新因子库必须复用）
- **70 个月逐月 IC**：2020-09 ~ 2026-06，逐月截面 Spearman（y2 = 未来 20 日超额收益，clip ±0.5）
- **增量 IC 口径**（Kronos 教训）：`增量 = 因子IC − max(基线因子IC)`，基线 = 均值回归 m120/c-1、反转 -past20——**只有独立增量才有价值**
- 脚本参考：`experiments/kronos/analyze_baseline.py`（可直接改造为通用因子评估器）
- 动态股票池（`output/backtest_v2/kronos_pools/` + `.stock_pool.json`）——避免幸存者偏差

---

## 3. 免费因子库资源（调研结论 2026-08-08）

| 资源 | 内容 | 获取 | 注意 |
|------|------|------|------|
| **Qlib Alpha158/Alpha360**（微软）| 158/360 个价量因子 | GitHub microsoft/qlib，`pip install pyqlib` 或直接扒 `qlib.contrib.data.handler` 的 Alpha158 代码 | ⭐ 首选：A 股适配、代码现成、自带评估工具 |
| **WorldQuant Alpha101** | 101 个公式化 alpha | GitHub 搜 "alpha101"（大量实现）| 美股思路，需 A 股化改造（涨跌停/复权/停牌），部分含市场中性假设 |
| **国泰君安 191 因子** | 短周期价量多因子（2017 报告）| 券商金工公众号 / 慧博投研 | 只有公式无代码，适合学方法 |
| **华泰金工多因子系列** | 估值/成长/动量/反转四类框架 | 同上 | 框架参考价值大 |
| **alphalens-reloaded** | 因子评估工具（IC/分层/换手）| `pip install alphalens-reloaded` | 与我们的逐月 IC 口径可互补 |

**重要提醒**：
1. 免费因子库大部分是**价量因子**（用 OHLCV 就能算）——我们项目独特优势在**扩展维度因子**（资金流/财务/股东/一致预期），这是免费库覆盖不到的
2. 因子**多重检验陷阱**：158 个因子各算一次 IC，5% 假阳性 ≈ 8 个"显著但纯噪声"——必须长周期 + 逻辑自洽 + 独立样本验证

---

## 4. 因子体系整体架构（目标态）

```
┌─────────────────────────────────────────────────┐
│ 组合层: 因子合成信号 → 选股 → 回测 → 模拟盘       │
├─────────────────────────────────────────────────┤
│ 因子评估层: 逐月 IC / ICIR / 分层 / 增量 / 正交化  │
├─────────────────────────────────────────────────┤
│ 因子库层: 统一因子注册表 + 统一计算接口           │
│   ├─ 价量类（Alpha158 + 自研）                   │
│   ├─ 估值类（PE/PB/PS/PCF + 预测PE）             │
│   ├─ 基本面类（ROE/毛利/负债/现金流）             │
│   ├─ 资金流类（主力/超大单/DDE）                 │
│   ├─ 情绪类（新闻/快讯/换手/波动）               │
│   └─ 事件类（预告/分红/股东变动）                │
├─────────────────────────────────────────────────┤
│ 数据层: sequoia_v2.db + extra_features + 股票池   │
└─────────────────────────────────────────────────┘
```

**与现有 V2 体系的关系**：因子体系不是替代 T2/T4，而是**上游**——产出的合成信号可以：(a) 作为新模型的特征输入；(b) 与 T2/T4 做 Rank 融合；(c) 独立成因子选股策略。评估口径与 V2 完全统一（70 个月逐月 IC）。

---

## 5. 构建流程（六步，每步带验证标准）

### Step 1：因子清单与计算引擎
- 建 `factors/` 包：每个因子一个函数（或类），输入 `(code, date)` 输出因子值
- **统一接口约定**：`factor(code, asof_date) -> float | None`（asof 对齐防 look-ahead）
- 先落地 3 类：Alpha158 价量（自动生成 158 个）、扩展维度因子（~30 个）、自研特征迁移（88 维中有解释性的转成因子）
- **验证**：抽查 20 只股票因子值与手工计算一致；空值率 < 20%

### Step 2：因子评估流水线（核心，全部自动化）
每月截面计算 → 70 个月 IC 序列 → 指标表：
```
对每个因子:
  ic_series[70个月]  → IC均值 / IC标准差 / ICIR / 正IC月占比
  分层测试[每月]      → D1~D10 单调性（D10-D1 均值）
  换手率[每月]        → 因子排序稳定性（月调仓换手）
  增量IC[每月]        → 因子IC - max(均值回归, 反转) —— 基线对照
```
- **验证**：全因子指标表生成；与 alphalens 结果交叉核对 3 个因子

### Step 3：因子筛选（过五关）
| 关卡 | 标准 | 淘汰含义 |
|------|------|---------|
| ① 显著性 | 正 IC 月占比 ≥ 60% 且 ICIR ≥ 0.5 | 不稳定 |
| ② 单调性 | D10-D1 价差显著且分层基本单调 | 排序能力弱 |
| ③ 独立性 | 与已有因子 |corr| < 0.7（先算相关性矩阵聚类）| 信息冗余 |
| ④ 增量 | 增量 IC ≥ +0.01（相对基线 + 相对同簇因子）| 无边际贡献 |
| ⑤ 逻辑自洽 | 因子经济学解释成立（如"股东减少→筹码集中→看涨"）| 纯统计巧合 |
- **验证**：通过率记录（通常 158 个 → 20-40 个幸存）；每关保留中间表

### Step 4：因子合成
- **正交化**：对幸存因子做主成分/回归残差化，消除共线性
- **加权**：ICIR 加权（V2 的 pred_std 动态加权思想复用）
- **合成信号**：因子值标准化后加权求和 → 每只股票一个分数
- **验证**：合成信号 IC 应 > 任何单因子；与 T2/T4 相关性 < 0.5（融合前提）

### Step 5：组合与回测
- 复用 V2 回测框架（M4 风控 + TOP_N）：合成信号 → 月度调仓 → 70 个月回测
- **门槛**：IC > 现有融合（+0.0165）且回测优于 +944.8% 基线（M4+TOP10）
- **验证**：与 V2 现有组合并排对比（收益/回撤/胜率/换手）

### Step 6：上线路径
- 因子库 → 月度信号 → 模拟盘（复用现有模拟盘模块）
- 每季度因子再评估（IC 衰减监测）——**因子有寿命，必须持续体检**

---

## 6. 评估与验证方法（详细）

### 6.1 逐月 IC（与 V2 口径完全一致）
```python
# 每月 ref 日（当月最后交易日）: y2 = 未来20日收益 - 沪深300同期收益, clip[-0.5, +0.5]
# IC = spearman(因子值截面, y2 截面); 70 个月得 70 个 IC
```
- 参考实现：`experiments/kronos/analyze_baseline.py`（改 `analyze()` 的 exp_ret 为任意因子值）

### 6.2 分层测试
- 每月按因子值分 10 组（D1 最低 ~ D10 最高），各组未来 20 日超额收益均值
- 合格：D10 > D9 > ... 大致单调，D10-D1 显著为正
- 注意：**分层用等权组均值**（月度调仓近似），不是个股收益

### 6.3 增量 IC（本项目独有口径，Kronos 教训总结）
- 基线因子：均值回归 `m120/c-1`、反转 `-past20`（免费可得，市场效应的代表）
- **增量 = 因子IC − max(基线IC)**；增量 ≥ +0.01 且跨月稳定 → 才承认因子的边际价值
- 原因：A 股短期收益大量被动量/反转效应解释，不带基线对照的"高 IC 因子"多数是这两个效应的镜像

### 6.4 多重检验校正
- 158 个因子 × 5% 假阳性 ≈ 8 个假显著——**不允许"显著就收"**
- 实操：幸存因子必须在**时间上切半**（前 35 个月发现，后 35 个月验证）——后半段仍显著才保留
- 更严：Bonferroni 或 FDR（BH 法）校正 p 值

### 6.5 过拟合防线（铁律）
- **三段数据**：因子发现用 2020-2023，验证用 2024-2025，样本外 2026（未来）
- 禁止：用全样本选因子再全样本报告
- 每次调整后必须重跑全流水线（脚本化，禁止手工拼凑）

---

## 7. 与现有项目的衔接（落地时）

| 复用资产 | 位置 |
|---------|------|
| 数据库/股票池 | `data/sequoia_v2.db`、`output/backtest_v2/.stock_pool.json`、`kronos_pools/` |
| 扩展维度 parquet | `data/extra_features/{subset}/{code}.parquet` + `sequoia_x/features_extra/build_extra_features.py`（33 维特征工程，asof 对齐已实现）|
| 评估口径 | `experiments/kronos/analyze_baseline.py`、`step3_analyze.py` |
| 回测框架 | V2 回测（BACKTEST_PLAN.md §4）、M4 风控、TOP_N |
| 融合思路 | ADR V3-01（独立性×强度）、增量 IC 门槛 |
| 监测/自动化 | nohup + 监测器 + 微信推送模式（断点续跑铁律）|

**新项目建议结构**（独立仓库）：
```
factor_lib/
├── factors/           # 因子实现（统一接口 factor(code, asof) -> value）
├── evaluate/          # 评估流水线（逐月IC/分层/增量/正交化）
├── data/              # 数据对接层（sequoia_v2.db + extra_features 只读）
├── report/            # 因子报告（HTML/Markdown）
├── config.py          # 配置
└── tests/             # 因子计算抽查测试
```

---

## 8. A 股数据条件与注意事项（坑清单）

1. **复权**：DB 为不复权价——因子计算前必须前复权（分红/送转会制造假动量）；或对除权日做标记处理
2. **涨跌停**：涨停日成交无量、次日行为特殊——因子（尤其量价类）在涨跌停日的值不可信，可剔除或置空
3. **停牌**：停牌股未来 20 日收益缺失——评估时剔除（与 step3 同逻辑）
4. **T+1 / 只能做多**：执行层约束，因子评估可忽略（组合层处理）
5. **Look-ahead 防护（最重要）**：任何因子只能使用 asof 当日及之前的数据——财务数据用**披露日**（不是报告期）、股东数据用**公告日**、一致预期用**快照日**（121 维特征工程已实现 asof 对齐，复用其逻辑）
6. **幸存者偏差**：用**按月动态股票池**（kronos_pools/），不能用今日全量回测历史
7. **amount 缺失**：腾讯源未入库，量价因子尽量用 volume；必须用 amount 时先 volume×均价 估算
8. **新股/次新**：上市 <60 日股票因子无意义（无 120 日窗口），剔除
9. **ST/退市**：因子计算前标记，评估时决定是否剔除（策略层决定，池子规则统一）

---

## 9. 路线图（分阶段，供新项目排期）

| 阶段 | 内容 | 预计 | 里程碑 |
|------|------|------|--------|
| P1 基建 | 因子包骨架 + 统一接口 + DB 对接 + 评估流水线（逐月 IC/分层/增量）| 2-3 天 | 能对任意自定义因子出 70 个月评估表 |
| P2 价量因子 | Alpha158 落地 + 自研动量/波动类 20 个 | 3-5 天 | 158 因子评估表 + 筛选（过五关）|
| P3 扩展维度因子 | 资金流/财务/股东/一致预期/事件 → 30 因子 | 3-5 天 | 扩展因子评估（复用 extra_features 数据）|
| P4 合成与融合 | 正交化 + ICIR 加权 + 与 T2/T4 融合对比 | 2-3 天 | 合成信号 IC > +0.0165 |
| P5 回测上线 | 70 个月回测 + 模拟盘 | 2-3 天 | 优于 V2 基线或同等回撤改善 |

**总投入**：约 2-3 周（一个人，CPU 服务器），关键路径是 P2（因子计算量大，需并行 + 断点续跑）。

---

## 10. 附录

### 10.1 术语表
| 术语 | 含义 |
|------|------|
| IC / ICIR | 见 §1（因子排序能力的均值与稳定性）|
| 分层测试 | 因子值分 10 组看未来收益单调性 |
| 正交化 | 消除因子间共线性（回归残差化）|
| asof 对齐 | 数据按"可得日"而非"报告期"使用 |
| 增量 IC | 因子 IC 减去市场效应基线（本项目口径）|
| 幸存者偏差 | 用今日存活股票回测历史导致的虚高 |

### 10.2 参考资源
- Qlib：github.com/microsoft/qlib（Alpha158 代码在 `qlib/contrib/data/handler.py`）
- alphalens-reloaded：github.com/stefan-jansen/alphalens-reloaded
- Alpha101 实现：GitHub 搜 "alpha101"（如 `wangleiphy/alpha101`）
- 券商金工：国泰君安《短周期价量多因子》、华泰《多因子系列》（公众号/慧博投研）
- 本项目参考：`experiments/kronos/analyze_baseline.py`、`BACKTEST_PLAN.md`、`V3研究方向与实验研究记录.md`（§11 融合分析/ADR V3-01/V3-17）

### 10.3 与本文档关联的记忆/教训
- 评估必须带基线对照（增量 IC）——Kronos 3a 证伪的核心教训（`kronos-ic-fake-signal-lessons`）
- 单月优势 = 噪声（方向一二三全部验证）——因子评估必须 70 个月口径
- 数据覆盖结论必须"随机抽样 + 独立源交叉 + 对照组"验证

---

> **后续行动**：本指南为规划文档。新项目启动时：git init 独立仓库 → 按 §9 路线图 P1 开始 → 每阶段完成后回填本指南 §5-6 的验证结果。评估口径一律复用 §6（与 V2 完全一致，保证可比性）。
